# MEMORY.md

## Long-term memory

- 用户偏好使用中文简体回复。
- 用户当前开发偏好：每个功能模块尽量使用独立的新 CSS / HTML / JS，不要再和旧代码混编；当前优先完成邮箱鉴权模块。
- 2026-03-21：开发规则进一步明确——已经实现业务的原始 CSS / JS / HTML 尽量不动；后续优先新增独立的 CSS / JS 文件承接改动，避免继续污染既有页面逻辑，降低把原始代码带坏导致程序异常的风险。
- 2026-03-21：替换策略规则——先通过新增文件并行复现原有逻辑，待新实现经过实际测试验证且程序无异常后，才允许删除或替换原来的旧文件；默认不直接覆盖旧实现。
- 邮箱鉴权模块代理策略偏好：全局代理与代理池都允许选择执行策略（轮询、随机）；临时自定义代理暂时不开放。
- 2026-03-16：完成初始 bootstrap，先采用默认身份设定：阿爪（赛博小助手，风格直接、靠谱、少废话，emoji 为 🦊）。后续可按用户意愿修改。
- 用户定义了“三层记忆机制”作为默认认知模型：第一层是会话 transcript（短期上下文，自动维护）；第二层是长期记忆 `MEMORY.md` + `memory/*.md`（偏好、决策、经验、项目知识，查询用 `memory_search`，读取用 `memory_get`）；第三层是 `shared-context/`（多 Agent 共享真相，含 `THESIS.md`、`FEEDBACK-LOG.md`、`knowledge-base/`）。后续默认按“会话记忆 → 长期记忆 → shared-context 共享对齐”的层级理解和使用。
- 2026-03-19：Java 技能体系已重构为稳定的五层结构：总入口（`java-knowledge-corpus`）、学习层（`java-learning-planner`）、表达层（`java-interview-answering`）、分诊层（`java-troubleshooting-triage`）、专项实施层（并发/JVM/数据/网络/Spring/设计重构）。总入口已瘦身，specialist 边界已收窄，并建立了 `_retired/recovered-to-specialists/` 与 `_retired/keep-archive/` 两层归档策略；后续以维护和小修为主，不再让总入口重新膨胀。
- 2026-03-21：AdsPower 配置页 `page-ops-v2.js` 的导出问题排查经验已确认：当“下载下来的根本不是相关文件”时，不能只盯文件生成逻辑，要优先检查导出失败时后端是否返回了 `200 + text/html` 错误页、前端是否把 HTML 当文件下载、以及导出接口是否对非法格式偷偷兜底。后续处理导出问题时，默认按“响应头/状态码校验 → 内容类型校验 → xlsx 内部结构校验（如 `xl/workbook.xml`）→ 前端下载与反馈链路”这一顺序排查，并确保导出失败返回明确的 4xx + 文本错误，不再回 HTML 页面。
- 2026-03-21：又确认一条导出交付经验：凡是用户明确需要“导出成真实文件”的功能，优先做后端真实附件下载（带正确 `Content-Type` + `Content-Disposition` 文件名），不要偷懒用前端 `Blob` + `a.download` 充数；否则在宿主环境/浏览器接管下载时，极易出现 UUID 随机文件名、扩展名表现异常、用户误判“不是目标格式”的问题。尤其是 json/txt/xlsx 这类格式，必须保证“导出什么格式，就落地成什么文件名和什么 MIME”。
- 2026-03-22：`D:\adspower-java` 已完成本机部署，确认 JDK 17、Maven 3.9.9、MySQL80 可用；Web 服务入口为 `http://127.0.0.1:8080/login` 与 `/app`，并已配置计划任务 `OpenClaw-AdspowerJava`、`OpenClaw-AdspowerJava-Ensure` 做开机启动与自恢复。
- 2026-03-22：用户管理/角色列表类问题的高频根因已确认：前端模板或脚本文件容易因尾部残留、重复拼接、乱码污染导致模块语法错误、版本号错乱或初始化中断。后续遇到“列表不加载/弹窗异常/页面逻辑突然挂掉”时，默认优先检查 HTML/JS 文件是否被拼接污染、脚本版本号是否一致，以及修改后是否已重新打包并重启，而不是先怀疑接口。
- 2026-03-22：用户管理页后续 UX 改造方向已明确——原生 `window.prompt` / `window.confirm` 是深色工作台风格不统一的核心障碍，后续应优先替换为页面内自定义深色确认/输入弹窗；批量分配角色能力也应避免默认覆盖，优先支持“追加到现有角色”为默认模式，并将“覆盖现有角色”设计为带明确二次确认的危险操作。
- 2026-03-23：AdsPower 后台页面架构迁移决策已明确：新实现优先落在 `/app` 新壳，不再继续依赖旧的 `/common/page` 壳承接新页面；`/`、`/login` 与已登录后的 `/common/page` 应逐步统一重定向到 `/app`，避免新旧壳并存导致入口混乱和重复维护。
- 2026-04-02：用户明确指定 `D:\Workspace\adspower-platform-v3` 为当前主要工作目录/主项目；后续未特别说明时，默认优先在该仓库上下文中理解需求、分析代码、执行改动与 Git 管理。
- 2026-04-02：`adspower-platform-v3` 已被设定为当前长期主战项目；后续需要持续熟悉系统各模块、前后端结构、启动链路、页面与接口映射、历史改造痕迹与高风险点，做到用户点到哪个部位就能直接在该仓库上下文里分析和修改。
- 2026-04-02：前端真源识别的首轮结论已确认：当前前端真实构建链以 `frontend/src/main.ts`、`router/index.ts`、`stores/auth.ts`、`.vue`、`.ts` 为主；`tsconfig.json` 已明确 exclude `src/**/*.vue.js`；仓库内大批 `.vue.js` / `.js` 更像历史并行产物、编译残留或迁移过渡文件，后续修改前端时默认优先检查并修改 `.vue` / `.ts` 真源文件，而不是 `.vue.js`。
- 2026-04-03：用户新增长期协作规则——凡是 `D:\Workspace\adspower-platform-v3` 这类主项目上的阶段性改造、结构迁移、关键排查结论、边界决策与下一轮入口，不应只留在聊天或当日日志里；后续默认同时沉淀到 `memory/YYYY-MM-DD.md` 与 `MEMORY.md`，把真正有长期价值的项目状态写成可续接的长期记忆，降低后续断档和“健忘”风险。
- 2026-04-03：Proxy Center 本轮拆分的阶段结论已明确：低风险 helper/builder 化已从 tab shell/dialog host/local coordination 继续推进到 Checks 与 Assets 两条主线。Checks 侧已收 create-job trigger-prep、polling-finish、selected-asset-check prep / trigger-prep；Assets 侧已收 delete flow、delete export、crud success/precheck/toggle、scope change，以及 load 主链外围的 query/state/apply/ui 四层语义。后续若继续推进，默认先把“准备态/成功态/收尾态”与真实请求/轮询主链分离，避免直接重构 polling 和 request orchestration。
- 2026-04-03：Proxy Center 当前高风险边界已进一步确认：`scheduleCheckJobListPolling(...)`、`pollAssetCheckJob(...)`、`createProxyCheckJob(...)` 后的真实调度链，以及 `loadAssets(...)` 更深层的统一 runner / 骨架重排，都属于下一阶段才考虑的高风险区；当前最合理停点是收口而不是继续硬拆。下次若继续，优先先做阶段对齐，再决定是否进入这些主链区域。
- 2026-04-04：Proxy Center shared detail 的渐进式收口路径已验证：先按 `detailType` 拆 host / builder，再统一 shell 的 open/close 入口，随后统一模板读取与 detail action 读取；当前已完成 `batch-delete-skip-summary` host 化，并在 `ProxyCenterViewV2.vue` 内落地 `openDetail(row)`、`closeDetail()`、`currentDetailType`、`currentDetailLines`、`currentStructuredDetail`、`currentDetailSummary`、`currentDetailFollowupHints`、`currentDetailAssetId` 等只读/入口层收口。
- 2026-04-04：shared detail shell 的一次性大改风险已被再次验证——在 `ProxyCenterViewV2.vue` 上直接把 `detailVisible/detailRow` 粗暴收成 `detailState`，很容易触发替换污染、`.value` 错位与编码风险。后续处理这类大体量 Vue 父文件时，默认采用“先出迁移清单 → 人工定点小步替换 → 每步独立提交”的方式推进，不再做大面积批量替换。
- 2026-04-04：当前 `ProxyCenterViewV2.vue` 的 shared detail shell 已达到阶段性健康停点：主要业务 detailType 已基本 host 化，打开入口与主要读取入口已统一，父层已从 detail 内容实现层收缩为 shared shell 容器/路由壳。现阶段不建议继续为形式统一而强行抽 resolver；除非后续 `detailType` 明显继续膨胀、需要跨页面复用同一套路由，或准备推进强类型 detail schema，否则优先停在现状更划算。
- 2026-04-11：`D:\playwright` 的 Dreamina + Firstmail + 代理注册主链已阶段性跑通，当前工作阶段已从“打通流程/修卡点”进入“性能精修 + 代理可用性治理”。主链关键结论已明确：Continue 后阻塞检查已清掉，验证码页统一等待已跑通，Firstmail latest 取码与 Dreamina 专用验证码输入已稳定可用，后续重点不再是能否注册，而是如何更快、更稳、减少坏代理浪费。
- 2026-04-11：Dreamina 验证码阶段的核心长期结论已确认：真正根因不是 Firstmail 或 Continue 流程，而是验证码候选池过宽会误选非验证码 DOM（日志已实锤命中过 `DIV.lv-menu-item lv-menu-item-size-default`），从而导致 fill/type/注入全部打空。后续维护 `D:\playwright\task-register.js` 时，默认必须保持 Dreamina 专用验证码白名单，不再回退到通用 textbox / 泛化 input / 任意可见 div 的宽泛候选策略。
- 2026-04-11：代理治理规则已升级：`预检通过 ≠ 业务可用`。`gate.ipfoxy.io` 这类代理已反复证明可能出现“基础预检通过，但 Dreamina 首页真实打开命中 `DREAMINA_FIRST_LOAD_DEAD_PAGE` / `DREAMINA_WHITE_SCREEN`”的情况。后续默认将这两类首页强失败视为业务不可用代理信号；运行期一旦命中，应像 IP 黑一样快速从健康池剔除并写入 bad，同时应补“轻量首页健康预检”作为正式批量注册前的第二层筛选，而不再只依赖简单 GET 连通性预检。
- 2026-04-11：当前性能优化方向的长期优先级已明确：首页阶段已通过资源拦截（run 默认拦 `image/media/font`）、`firstLoadHealth` 的 run/test 分离、登录信号等待 run/test 分离等手段明显压缩；下一阶段的主要优化焦点转为后半段时延与代理筛选质量，尤其关注 Firstmail 轮询节奏、session cookie 建立等待，以及把页面级/代理级失败原因保留下沉给 runner 正确淘汰坏代理。
- 2026-04-12：用户对 Dreamina staged architecture 下达了新的长期协作要求：从“查看整套架构、检查有没有不用的/死循环逻辑、哪些文档注释不完善、哪些边界没有及时更新”这轮系统审计开始，后续相关架构整改结论、主路径冻结决策、边界更新和文档收口内容，都必须同步沉淀到长期记忆，不能只留在聊天里。
- 2026-04-12：Dreamina staged architecture 本轮阶段性总判断已明确：当前不是推倒重来，而是进入“主路径冻结 + 临时桥接收口 + 文档追平代码”的整改期。verification 当前应冻结为 `dreamina-direct-fill` 主路径；profile-completion 当前应冻结为 `fillDreaminaBirthdayContinuousFlow` 主路径；birthday 业务流按 `Year -> Month -> Day -> Next` 连续执行；旧验证码 fallback（char-by-char / hidden-input / wrapper-keyboard / fallback-keyboard-type）后续默认降级为 debug-only，不再自动参与主链。
- 2026-04-12：Dreamina 注册链当前已验证的重要真实进展：verification 通过 `direct-fill` 已可把流程推进到 birthday / profile-completion 页面；当前主要风险点已从验证码“完全打不通”前移为 birthday 连续动作流之后的 confirm/post-auth-ready 收口，以及 Dreamina-register / shared-verification / shared-profile-completion 文档与代码不同步的问题。后续整改优先级按“P0 主路径冻结与责任统一 → P1 resend/debug-only/依赖收口 → P2 文档与边界格式统一”执行。
- 2026-04-12：Dreamina birthday 长期规则再次确认并应视为默认常量：出生年月日必须是“随机 + 满 18 周岁”。当前 shared-profile-completion 生成的 birthday plan 已带 `ageQualified=true`；后续任何 birthday 相关实现或排障都默认遵守这条规则，不再重复争论。
- 2026-04-13：对 `D:\playwright\Dreamina` 及 6 个 shared stage 包完成整套 staged architecture 静态审计后，当前最核心的长期判断进一步明确：真正的问题不是死循环，而是第 4/5/6 阶段已进入“主路径冻结后，代码与文档仍停留在过渡桥接期”的错位状态。第 4 阶段主路径虽已冻结为 `fillDreaminaBirthdayContinuousFlow`，但公共骨架仍残留未收干净的 split-flow 中间态；第 5 阶段名义上是 `post-auth-ready`，实际 Dreamina profile/adapter 仍大量依赖 birthday 页元素作为 ready/bridge 信号；第 6 阶段名义上是 `account-delivery`，实际 Dreamina 仍主要依赖 `email + url/account context` 做最小交付收口。后续整改应默认先修 P0 硬伤，再把第 5/6 阶段明确标注为 bridge/minimal 模式或补齐真实已登录态/交付态信号，避免文档先毕业、代码仍桥接。
- 2026-04-13：本轮审计已确认一个必须优先处理的代码级硬伤：`shared-profile-completion/stages/profile-completion-submit.js` 内存在未定义变量 `hasSplitFlow` / `hasContinuousFlow` 的直接使用，存在运行时 `ReferenceError` 风险；后续只要继续维护 Dreamina 第 4 阶段公共骨架，默认先检查并消除这类“主路径已改、公共层中间态变量未收口”的残留。
- 2026-04-13：用户明确划定 `D:\playwright\config.json`、`D:\playwright\dreamina-health.js`、`D:\playwright\dreamina-register-profile-loader.js`、`D:\playwright\dreamina-register-profile.json`、`D:\playwright\firstmail-api.js`、`D:\playwright\proxies.txt`、`D:\playwright\proxy-precheck.js`、`D:\playwright\runner.js`、`D:\playwright\task-register.js`、`D:\playwright\results` 为 Dreamina 老系统架构边界；后续必须默认把这些文件/目录视为旧架构资产，不再把它们当作新 staged architecture 的正式运行入口或主承载层来建议。新架构主入口与正式运行语境应优先落在 `D:\playwright\Dreamina` 目录及各 `shared-*` stage 包上，避免再次把新旧运行体系混说。
- 2026-04-13：用户进一步明确划定 `D:\playwright\Dreamina`、`D:\playwright\shared-account-delivery`、`D:\playwright\shared-credential`、`D:\playwright\shared-entry`、`D:\playwright\shared-post-auth-ready`、`D:\playwright\shared-profile-completion`、`D:\playwright\shared-proxy-precheck`、`D:\playwright\shared-verification` 为 Dreamina 新系统架构边界；后续默认应把这些目录视为新 staged architecture 的正式主干、运行入口语境和长期演进承载层。凡是讨论新架构运行、阶段编排、日志体系、阶段耗时、结果交付、边界整改与后续演进时，必须优先以这组目录为准，不再与老架构目录混淆。
- 2026-04-13：用户已明确要求将 `D:\playwright` 设为当前长期主项目目录；后续若未特别说明，默认优先在 `D:\playwright` 上下文中理解需求、分析代码、执行改动与 Git 管理。
