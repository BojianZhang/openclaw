# 2026-03-14

- Worked in `D:\adspower-java` on a Spring Boot + Thymeleaf dashboard for oiioii / AdsPower automation.
- Completed proxy-aware backend verification and committed: `b0b05e3 feat: finish proxy-aware api modules`.
- Fixed dashboard IP-check wiring so `/network/check-ip` uses the current proxy mode stack correctly; committed: `b6456b1 fix: wire ip check to proxy modes`.
- Built batch invite-signup workflow with custom account format parsing, default support for `{{email}}:{{password}}`, username derived from email local-part with digits removed; committed: `04b9fcb feat: add batch invite signup workflow`.
- User confirmed common account format example: `johnunderwood1907@shoulaqil.com:zudlvtizA!4394`; username rule is email local-part stripped of digits.
- Adjusted main automation flow to prioritize: batch signup -> fetch latest mail / extract verify link -> open verify link; invite-user-by-email kept as fallback only.
- Added streamlined verification flow in dashboard UI, including batch mail fetch/open-link actions and fallback copy updates; committed: `4027126 feat: streamline signup verification flow`.
- Added retry + export controls for verification flow results and clarified fallback confirmation-mail wording; committed: `a1cee99 feat: add retry and export for verification flow`.
- Converted batch mail-fetch and open-link into async task workflows with per-stage progress, failed-stage reporting, filters, field-copy helpers, and queue-aware status polling; committed: `2d11fd7 feat: async signup verification pipeline`.
- Added adaptive task concurrency planning based on JVM memory/CPU plus manual overrides in the UI; current observed recommendations on this machine were roughly mail fetch concurrency 10 and open-link concurrency 3; committed: `56aceb7 feat: add adaptive task concurrency planning`.
- Added global signup task queue with running/queued limits, queued state, and queue status endpoint/UI; committed: `488af2a feat: add global signup task queue`.
- Added queue management panel with task list, cancel queued task, clear queued tasks, and clear finished history; committed: `07fc734 feat: add queue task management panel`.
- Added one-click full signup pipeline button that chains signup -> mail fetch -> open links using existing task states; committed: `e4a7c99 feat: add one-click signup pipeline`.
- Surfaced stage-specific proxy strategy in the UI so signup, mail-fetch, and Playwright/open-link stages clearly show independent proxy/IP strategy selection; committed: `100f58a feat: surface stage-specific proxy strategy`.
- Important product direction from user: automation needs stage-specific IP/proxy configuration, and likely future work is per-stage / later per-account proxy-pool distribution.
- Continued work in `D:\adspower-java` on invite-code and invite-claim business semantics. User required invite-code state to be split into 3 business states: 未使用 / 已注册未登录邮箱确认 / 已经使用, with query logic, page stats, and cross-module passing aligned to that distinction.
- Updated `src/main/resources/templates/dashboard.html` so invite-code UI now shows 3 single-result state buckets and 3 aggregate batch-result counts. Current frontend state rule implemented: `usedAt` present => 已经使用; else `uid` present => 已注册未登录邮箱确认; else => 未使用.
- Redeployed updated jar to `D:\adspower-java\deploy\adspower-java-1.0.0.jar`, restarted the Windows process on port 8080, and revalidated `http://127.0.0.1:8080/dashboard/automation` returning HTTP 200.
- User supplied decisive business correction for Invite Friend Claim: reward collection is driven by `POST https://api.oiioii.ai/points/add` with body `{ "data": { "taskId": "...", "type": "invite_friend" } }` plus Bearer token. Weekly Task `taskId` is the key reward input; invite code itself is not the primary claim parameter.
- Based on user screenshots, future Invite Friend Claim refactor should pivot from “token + invite code claim” to “token + taskId + type=invite_friend”, with eventual batch flow likely being: token set -> discover claimable Weekly Task ids -> pair token/taskId -> batch claim.
- User asked to pause after this clarification; no further changes should be made until they resume.
