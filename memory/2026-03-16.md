- adspower-java front-end was heavily restructured from old dashboard single-page architecture into a new `/app` modular workspace with separate pages, services, core helpers, components, and Thymeleaf fragments.
- Legacy dashboard flow was retired: `/dashboard` now redirects to `/app`; legacy dashboard support/template code was removed, and redirect helper logic was separated into `RedirectSupport`.
- New frontend architecture docs were added to `D:\adspower-java`: `FRONTEND_ARCHITECTURE.md`, `UI_MODULE_GUIDE.md`, and `REPO_CLEANUP_TODO.md`.
- Major template-system milestone: automation pages now use shared Thymeleaf fragments (`form-blocks.html`, `layout-blocks.html`) and several pages were converted to `*-blocks.html` + `twoPanelWorkspace` composition.
- Repository cleanup was started safely: removed root `tmp*` scripts and `run-web*.log` files; updated `.gitignore` to ignore `/tmp*` and `/config/`.
- OiiOii config page UX direction was clarified by the user: keep the main page module cards fixed-height so the whole page stays compact, and provide an expand modal for every module so full parameters can be viewed/edited in a large panel instead of forcing long in-card scrolling.
- During OiiOii page debugging, the root cause of missing expand buttons was confirmed to be stale/partially-updated runtime templates under `D:\adspower-java\target\classes`, not just browser cache. Verifying the actual running HTML was necessary.
