- 用户再次明确开发原则：每个功能模块都必须走独立的新 CSS / HTML / JS / 后端服务，不在老代码上修改，必须避免和老项目耦合。
- 当前在审阅/规划的项目路径是 `D:\adspower-java`，当前首要关注模块是 Playwright 登录。
- 用户对 `D:\adspower-java` 中 Playwright 登录模块的产品边界做了明确约束：`配置中心 -> OiiOii 配置 -> Playwright 登录配置` 只负责规则配置；`Playwright 登录模块` 才是具体执行者与把控者。
- 用户已明确 OiiOii 配置中 Playwright 登录规则链路：打开 `https://www.oiioii.ai/`，点击登录/Log In，填写邮箱与密码（来自邮箱:密码列表），勾选“我同意服务条款与隐私政策 / I agree to the Service Terms and Privacy Policy”，再触发登录按钮；提交时必须做好多策略点击容错；登录成功后以“我的资产 / My Assets”文本或对应元素存在作为成功判定，否则视为失败并截图溯源。
- 用户已明确 Playwright 登录业务要求：
  1. 规则从 OiiOii 配置读取；
  2. 代理配置从配置中心代理配置读取，支持策略如轮询/随机；
  3. 并发推荐数要依据服务器性能动态推荐，并按推荐并发异步执行；
  4. 输入为邮箱:密码列表；
  5. 执行前必须做严格代理校验，并采用“代理池 -> 全局代理 -> 本机直连”的回退链路；
  6. 代理校验通过后按 OiiOii 配置规则执行；
  7. 执行完成后必须保留成功/失败结果、失败原因、失败阶段、失败重试能力，并支持批量导出；失败任务重启只能对当前列表追加，不能刷新页面导致业务数据丢失；
  8. 需要实时展示当前并发执行列表与执行阶段反馈。
