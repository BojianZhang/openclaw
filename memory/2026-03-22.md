- 2026-03-22：已在 `D:\adspower-java` 完成本机部署，确认 JDK 17、Maven 3.9.9、MySQL80 可用；Web 服务可通过 `http://127.0.0.1:8080/login` 与 `/app` 访问，已启用计划任务 `OpenClaw-AdspowerJava` 与 `OpenClaw-AdspowerJava-Ensure` 做开机启动与自恢复。
- 2026-03-22：排查“用户管理/用户角色列表异常”时确认根因是前端脚本 `src/main/resources/static/system/js/system-users-workspace.js` 文件尾部存在残留拼接代码，导致模块语法错误、页面初始化中断；修复后需重新 `mvn -DskipTests package` 并重启服务，单改源码文件不会影响正在运行的 jar。
- 2026-03-22：继续按既定前端改造规则执行——尽量不动既有业务逻辑，优先通过新增独立 CSS/模板承接界面升级。已新增 `system-roles-workspace-v2.css`、`system-menus-workspace-v2.css`，并重构 `templates/system/roles.html`、`templates/system/menus.html` 为更接近 AdsPower 风格的深色工作台布局，同时保留原有 JS 绑定和接口结构。
- 2026-03-22：用户管理页弹窗样式统一时，已确认当前最大问题不是普通 CSS 细节，而是仍在使用 `window.prompt` / `window.confirm` 的原生浏览器弹窗，导致视觉风格无法统一；后续需要优先替换为页面内自定义深色确认/输入弹窗。
- 2026-03-22：关于“批量分配角色”，已确认当前实现属于“直接覆盖最终角色集合”，对已有角色用户有误覆盖风险；后续应改为支持三种批量模式：`追加到现有角色`（建议默认）、`覆盖现有角色`、`从现有角色中移除`，其中“覆盖”必须带明确二次确认。
