- 2026-03-24：本轮在 `D:\Workspace\adspower-platform-v3` 做了前端收尾优化：系统路由改为懒加载，`frontend/vite.config.ts` 增加了较稳的 manualChunks 策略（`element-plus` / `vue-core` / `vendor`），最终按页面自然拆包，循环 chunk 告警已消失，但 `element-plus` 仍是主要大包来源。
- 2026-03-24：用户已明确反馈“整个皮肤大面积崩溃”；高概率根因是最后一轮对 `frontend/src/styles/base.css` 的激进重写破坏了原先依赖 `base.css + menus-dark.css + 页面 scoped 样式 + Element Plus 覆盖` 共同维持的深色主题接管链，导致大量页面回退为浅色/白底。
- 2026-03-24：后续修复策略已明确：优先止血，只回退/修复全局皮肤层（尤其 `frontend/src/styles/base.css`，必要时少量补 `menus-dark.css`），不动已完成的业务功能、审计能力、菜单/用户模块逻辑和路由懒加载/拆包优化；后续 UI polish 默认只做页面 scoped 小修，避免再次大改全局样式基座。
