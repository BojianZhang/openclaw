- 2026-03-25：AdsPower Java 后台“统一页面高度”问题已开始收口处理，优先方案不是改业务结构，而是先统一 `/app` 主壳与 system 嵌入页的高度口径：`app-shell-v2.css` 中 `workspace-main` 改为固定 `height: 100vh + overflow: hidden`，`system-management.css` 中嵌入页 body / main 改为继承父容器高度并让滚动落在内部内容区；用户管理、角色管理、菜单管理子页根容器已从各自 `100dvh` 往父容器 `height: 100%` 收口，后续若仍有页面滚动条过长，优先继续检查具体长表格区 / 空白区的内部滚动分配，而不是重新放大全页高度。
