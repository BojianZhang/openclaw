- 2026-04-09：在 D:\playwright 的 Dreamina 注册流程中，用户明确要求将“真实注册”与“代理预检”在日志上强区分，并必须展示“当前账号 + 代理网关 + 实际代理出口 IP + 是否有头浏览器”。后续若继续维护该批量注册器，默认保持这一可观测性标准。
- 2026-04-09：Dreamina 登录页存在“入口元素（如 Sign in）出现慢”和“Something went wrong / Refresh”异常弹窗两类高频波动。当前协作方向已明确：等待登录入口必须采用更宽松的自适应等待；检测到异常弹窗时优先自动点击 Refresh，必要时整页 reload；同时要设置连续异常恢复上限，避免无限刷新死循环。
- 2026-04-09：Dreamina 异常恢复策略已被用户要求做成可配置项，至少包括 `dreaminaMaxRecoveries` 与 `dreaminaRecoveryBonusMs`；后续遇到类似等待/恢复类参数，优先落到 D:\playwright\config.json，而不是硬编码在脚本里。
- 2026-04-09：本轮排查确认过一个真实代码坑：`task-register.js` 中 `openDreaminaWithRetry(...)` 使用 `config.dreaminaMaxRecoveries` / `config.dreaminaRecoveryBonusMs` 时，如果未把 `config` 参数传入，会直接触发 `config is not defined`。后续在该项目里加配置透传时，默认先核对函数签名和调用链，避免作用域遗漏。
- 2026-04-09：用户新增需求——批量注册器需要支持“多并发”，并且并发数量必须通过 `D:\playwright\config.json` 配置，而不是写死在代码里。
- 2026-04-09：本轮已对 D:\playwright 项目完成一轮工程化整理：新增 README.md 作为正式项目架构与使用说明文档；将 config.json 调整为更稳的排障默认值（roundRobin、concurrency=2、waitMailAttempts=18、headless=false、slowMo=120）；为 package.json 补齐 start/run/precheck/reset/reset:all/test/report 等 scripts；新增 reset-results.ps1 安全清理运行产物（results/screenshots/storage，默认不动账号、代理、配置与源码）；并在 README 中同步补齐 reset 使用说明与常用命令。
- 2026-04-09：D:\playwright 项目 Git 初始化协作规则已明确：应使用 .gitignore 排除运行产物与敏感输入，至少包括 node_modules/、results/、screenshots/、storage/、test-results/、playwright-report/、user.json、accounts.txt、proxies.txt 以及 dreamina-*.png；并提醒用户 .gitignore 仅对未入索引文件生效，若已 git add 过，需要先执行 git rm -r --cached -f . 再 git add . 重新整理暂存区。
- 2026-04-09：D:\playwright 当前优先修正的两项结果正确性规则已明确并落地：一是 Dreamina 注册成功后的账号 session 提取必须只认 Dreamina 域下 cookie 名 `sessionid`，不能再回退混用 `msToken`、`sid_guard`、代理 `sessid` 等其他字段；二是 Firstmail `messages/latest` 的验证码轮询应与页面验证码阶段联动，默认先等待页面出现类似 “Resend code in XXs” 的倒计时元素，再开始 API 轮询，避免过早盲拉验证码。
