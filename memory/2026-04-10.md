- 2026-04-10：用户当前把工作目录切到了 `D:\playwright`，本轮主要在该仓库上处理 GitHub 提交、README 同步和 Playwright 注册脚本问题。
- 2026-04-10：`D:\playwright` 已绑定远程仓库 `https://github.com/BojianZhang/playwright.git`，当前主要开发分支为 `dev`；本轮已完成版本发布相关动作：推送代码、更新版本到 `v1.0.1`、创建 Git tag `v1.0.1`。
- 2026-04-10：`D:\playwright\README.md` 已重写为更适合 GitHub 公开仓库展示的正式版，并额外做了一轮用词润色；本轮曾提交 `docs: refine README`。
- 2026-04-10：排查 `D:\playwright` 的 Dreamina 注册流程后，确认高频问题不只一种：包括“账号已存在（An account with this email already exists）”、“验证码错误（Wrong verification code. Try again.）”以及生日页 `Next` 选择器误匹配 `Next one` 导致的 strict mode 报错。
- 2026-04-10：`D:\playwright\task-register.js` 已完成一轮定点修复：新增识别 `ACCOUNT_ALREADY_EXISTS`、新增识别 `WRONG_VERIFICATION_CODE`、将生日页下一步按钮收紧为 `button.lv_new_sign_in_panel_wide-birthday-next`、验证码阶段识别不再只依赖倒计时文案，也接受验证码输入框出现；本轮曾提交 `fix: detect signup/code errors and tighten birthday next selector`。
- 2026-04-10：用户对 `D:\playwright` 的运行速度优化提出了明确规则：测试阶段保留拟人化，非测试阶段走极速模式。已在 `config.json` / `task-register.js` 中落地运行模式分流：`runMode=test` 时保留 `testSlowMo` 与 `testHumanPause*`，`runMode=run` 时使用 `runSlowMo=0` 与 `runHumanPause*=0`；截图也按运行模式控制，测试模式默认可开，运行模式默认关闭。
- 2026-04-10：`D:\playwright` 本轮继续强化代理预检与代理池使用策略：预检目标已升级为双目标（主目标 `https://www.google.com/`，副目标 `https://dreamina.capcut.com/ai-tool/home`），并在 `proxy-precheck.js` / `config.json` 中新增速度分层配置 `proxyPrecheckFastThresholdMs`、`proxyPrecheckSlowThresholdMs`，将代理按 `FAST/NORMAL/SLOW/UNREACHABLE` 标记写入 `results/proxy-ok.txt` 等结果文件，同时记录 `primaryDurationMs`、`secondaryDurationMs`、`exitIpDurationMs`。
- 2026-04-10：用户明确要求按运行模式切换代理速度档位：测试模式使用 `FAST + NORMAL`，运行模式只使用 `FAST`。已在 `D:\playwright\config.json` 中加入 `testProxySpeedTiers=["FAST","NORMAL"]` 与 `runProxySpeedTiers=["FAST"]`，并在 `runner.js` 中落地按 `runMode` 过滤 `results/proxy-ok.txt`；因此当前主流程若存在 `results/proxy-ok.txt`，会优先从其中读取并按速度档筛选代理，而不是直接读取 `proxies.txt`。
- 2026-04-10：`D:\playwright` 当前又确认了两条 Dreamina 注册链路的重要经验：一是点击 Continue 后不能只做短暂点状检查，必须实现一个持续数秒的“结果判定窗口”，优先等待 `ACCOUNT_ALREADY_EXISTS`、`SIGNUP_REJECTED`、验证码输入框/倒计时等结果信号，谁先出现就立即走对应分支并结束当前浏览器；二是对“首页 URL 已打开但首轮加载后仍无正常页面元素”的伪加载/死页状态，不能继续等待或重试太久，应直接判定为强失败并把当前代理打入不可用池，避免白页代理反复进入主流程。
- 2026-04-10：`D:\playwright` 的多窗口显示问题已定位到窗口布局算法边界控制不足，而不只是窗口大小选择问题：统一网格布局如果没有对 `x/y` 和最终宽高做屏幕可视区域裁剪，在分辨率不足或系统缩放影响下会把第三行/末行窗口排到屏幕外，表现为窗口互相压住、只露半截或飞出底部。后续处理窗口布局时，默认要同时检查列数策略、最小窗口尺寸以及屏幕边界裁剪是否齐全，而不是只调 `preferredColumns`。
