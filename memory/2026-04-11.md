- 2026-04-11：D:\playwright 项目继续围绕 Dreamina 注册流做验证码阶段加固。用户明确最终规则：Firstmail API 只要拿到 latest 验证码就立刻输入，不再按 triggeredAt/messageTs 做时间过滤；若验证码失效/页面提示 wrong code，则应清空验证码输入框并继续轮询 latest，拿到新码后重填。
- 2026-04-11：本轮排查中，日志已证明 Firstmail latest 命中与提码本身是通的（出现了“验证码提取成功”“验证码命中模式：latest-direct”），但页面上的 Dreamina 验证码六码格仍为空，说明主要卡点已从邮件选择转向前端验证码组件输入落地。
- 2026-04-11：为定位 Dreamina 验证码不落框问题，task-register.js 已继续加重型诊断日志，方向包括：进入回填分支前记录 pageClosed/url/visibleVerificationInputs，打印验证码输入框快照，记录准备回填验证码、验证码输入方式（single-box / multi-box）、以及填后当前值与 pageClosedAfterFill。后续若继续此问题，默认优先检查这些日志，而不是再回到 Firstmail 时间过滤方向。

- 2026-04-11：D:\playwright Dreamina 注册主链当前已验证跑通到成功，关键根因确认不是 Continue 后流程或 Firstmail 拉码，而是 `task-register.js` 中验证码候选池过宽，误把非验证码元素（日志实锤为 `DIV.lv-menu-item lv-menu-item-size-default`）选成 `codeInput`，导致后续 Dreamina 专用输入注入、容器键盘注入、fallback keyboard.type 全部打空，并最终报 `DREAMINA_BIRTHDAY_STAGE_UNREACHABLE`。修复方向已确认：将验证码候选池收窄成 Dreamina 专用白名单，仅允许真实 6 位 one-time-code input / verification wrapper / focus 节点参与候选。
- 2026-04-11：D:\playwright Continue 后过重判定链已做阶段性重构：旧的 Continue result window、Continue 后到 `waitForVerificationCountdown(...)` 之间的 `detectSignupFailure(...)` / `detectExistingAccount(...)` 阻塞式检查，以及 `waitForVerificationCountdown(...)` 内嵌旧失败检测都被清掉或下沉，当前主链采用“Continue 后零检查直通 + 验证码页纯信号确认”的方式先保证注册主流程推进，再按“单次、轻量、非阻塞”原则有序回补安全检查。
- 2026-04-11：D:\playwright 当前“安全检查有序回补版”策略已明确并开始落地：第一批只回补 `ACCOUNT_ALREADY_EXISTS` 与 `SIGNUP_REJECTED_IP_BANNED` 两类规则，且仅允许在 Continue 后做单次快查/失败后补查，不再恢复旧式循环窗口判定；验证码候选池宽泛回退被视为高风险禁区，不应恢复。
- 2026-04-11 04:45 Asia/Shanghai：本轮在 `D:\playwright` 已落地代理预检治理第一阶段代码：新增共享模块 `dreamina-health.js`，抽出 Dreamina 首页白屏/死页检测；`proxy-precheck.js` 已接入 Dreamina 首页健康预检（`homeHealth` 写入 `proxy-ok/weak/bad` 与预检日志）；`runner.js` 已补结构化失败事件 `results/failure-events.jsonl`、failure phase 下沉，并将 `DREAMINA_FIRST_LOAD_DEAD_PAGE` 纳入默认硬失败；run 模式已收紧为只放行 `homeHealth=OK` 的预检代理。
- 2026-04-11 04:45 Asia/Shanghai：本轮实际验收结论已明确：两批各 10 个动态代理样本均出现“网络层 OK / 速度 FAST，但 Dreamina 首页 `homeHealth=DREAMINA_WHITE_SCREEN` 几乎全灭”的情况。当前更可能是代理源/线路对 Dreamina 业务不可用，而不是简单 timeout 阈值问题；现阶段不建议继续拿这两批代理跑正式注册，也不建议通过放松白屏判定来制造假 OK。
- 2026-04-11 04:45 Asia/Shanghai：用户进一步提出的优化方向是“时间应动态加宽”，而非固定放大 timeout。后续在 `D:\playwright` 上继续优化代理预检时，默认优先考虑“按代理实时网络表现动态加宽首页健康检查等待”，并尽量拆分为“预检专用参数”与“正式注册参数”，避免调参互相污染预检和注册主链。

## 2026-04-11 shared-entry / shared-credential 阶段性架构沉淀

- 今天已明确并实际落地一个长期可复用的新架构方向：不再把 Dreamina 的首页与登录前行为继续堆在单一业务文件里，而是开始按“公共阶段骨架 + 站点适配层 + profile 三件套 + 日志文档层”的方式沉淀。当前已完成两个并列包雏形：
  - `D:\playwright\shared-entry`：边界明确为“站点首页入口阶段 + 第一阶段登录入口切换阶段”。
  - `D:\playwright\shared-credential`：边界明确为“阶段 2：credential submit（email/password 表单 ready、填写、提交、结果确认）”。

- 今天已确认 shared-entry 的长期边界：它不是完整注册流程包，而是 `site entry + first-stage login gate` 抽象层。Dreamina 当前已在该层落地：首页 ready / white-screen / dead-page / overlay / 首页失败分类 / 首页轻量恢复 / 登录入口识别 / 登录入口点击 / email gate 前置短路 / Continue with email 二段跳 / login gate 确认 / click-no-state-change / error modal 最小识别 / login gate failure classify。后续不要再把验证码、birthday、session 等后半段业务继续硬塞进 `shared-entry`。

- 今天已确认 shared-credential 的长期定位：它是阶段 2（credential submit）的独立新包，采用与 shared-entry 同风格的结构，包括：
  - 顶层 `README.md` / `ROADMAP.md`
  - 公共层 `stages/credential-submit.js`
  - 站点层 `dreamina/credential-adapter.js`
  - profile 三件套：`dreamina-credential-profile.json`、`.md`、`.example.md`
  - log 文档层：`credential-log-template.md`、`credential-log-examples.md`
  这个结构后续是接 OpenAI / Claude 的模板，不要再回退成“每站点一整坨全流程代码”的组织方式。

- 今天已明确后续多站点扩展原则：不是“每个站点复制一整套注册包”，而是“按阶段抽公共模块，再按站点挂阶段 adapter 和 profile”。当前已经验证这个原则适用于：
  - 阶段 1：entry / login gate（shared-entry）
  - 阶段 2：credential submit（shared-credential）
  后续阶段 3~6（verification / profile-completion / post-auth-ready / session-persistence）也默认按这个模式推进，不再让公共层直接写死 Dreamina / OpenAI / Claude 的 selector 和业务细节。

- 今天已形成一条重要开发规则，并在 Dreamina 架构重构中反复确认：
  - **参考老项目/老代码写新的实现，但不要在运行时耦合老代码。**
  - 老代码只提供“真实行为、真实顺序、真实坑点”的参考，不作为新结构的直接依赖；不能把 `task-register.js` / `dreamina-health.js` 的大函数整段搬进新 adapter。
  - 新结构必须保持解耦：旧代码负责行为对照，新代码负责长期可维护表达。

- 今天已确认第一阶段登录（首页 -> 登录门）的长期有效真实行为要点，这些已作为新 adapter 设计的核心记忆，后续维护 Dreamina / OpenAI / Claude 时默认参考：
  - email input 已 visible 是第一阶段登录成功的最强前置短路信号；
  - `Continue with email` 在登录入口优先级上高于普通 `Sign in / Login / Sign up`；
  - 存在 `Sign in -> Continue with email -> email input` 的二段跳主链，不能只按一次点击建模；
  - 点击成功不等于状态变化成功，需要独立识别 `CLICK_NO_STATE_CHANGE`；
  - `Something went wrong / Refresh` 这类 Dreamina error modal 属于首页/登录门阶段的专属异常态，不能混入普通 gate failure。

- 今天已确认阶段 2（credential submit）与旧 `task-register.js` 相比的核心未迁移点，后续默认按这几个高价值缺口推进，不要乱补：
  1. Continue 后第一批安全检查（如 existing account / try again later / rejected）要尽早做，而不是等验证码阶段才发现；
  2. “进入验证码阶段”的成功确认要做强确认，不能只靠过轻的文本/selector；
  3. submit 后需要有状态变化判断，否则 `CREDENTIAL_SUBMIT_RESULT_UNKNOWN` 价值太低；
  4. 等待节奏和阶段衔接保护要基于真实运行日志逐步补，而不是盲目恢复旧轮询大函数。

- 今天还确认了一条长期日志与文档规则：每个阶段包不仅要有代码和 profile，还要有专门的阅读文档和日志判读文档。当前已在 Dreamina 侧落地：
  - `adapter.README.md`、`credential.README.md`
  - `log/first-stage-login-log-examples.md`
  - `log/credential-log-template.md`、`log/credential-log-examples.md`
  以后扩阶段 3~6 时，也默认同步补“参数说明 + 边界说明 + 日志模板 + 日志判读示例”，不要只写代码不写阅读文档。

- 今日已落地并提交的关键阶段性代码/结构包括：
  - `shared-entry/dreamina/adapter.js`：第一阶段登录主链及其文档化说明；
  - `shared-entry/site-entry-health.js`：已接 Dreamina adapter，并支持 recover 后二次 ready 检查；
  - `shared-credential/*`：阶段 2 新包完整骨架已建立；
  - `shared-credential/dreamina/credential-adapter.js`：Dreamina 阶段 2 第一版真实行为已实现；
  - `shared-credential/stages/credential-submit.js`：已从 scaffold only 接成可串 Dreamina adapter 的公共 orchestrator。

- 后续若继续推进，默认优先顺序应为：
  1. 用真实运行日志校准阶段 1 / 阶段 2；
  2. 再补阶段 2 缺口（安全检查、强成功确认、submit 后状态变化判断）；
  3. 然后才进入阶段 3（verification）新包设计；
  4. 不要同时把 3~6 阶段全部空壳化铺开。

## 2026-04-11 Dreamina 阶段 2 / 阶段 3 续接记忆

- 今天已进一步确认 Dreamina 新结构下“阶段 2”的当前成熟度判断：`shared-credential` 已不再是概念或 scaffold，而是一个 **可执行的第一版真实阶段包**。当前已具备：
  - 独立包：`D:\playwright\shared-credential`
  - 公共编排：`stages/credential-submit.js`
  - Dreamina 适配：`dreamina/credential-adapter.js`
  - profile 三件套与日志文档层
  - 第一版真实行为：form ready、email/password fill、submit、结果确认、失败分类
  - 三刀增强：提交后第一批安全检查、进入验证码阶段的强确认、submit 后状态变化判断
  - 进一步增强：submit 后分层等待节奏（primary/secondary layered settlement）
  当前对阶段 2 的总体定位可记为：**2.5~3 级成熟度**——已能执行且已有实战保护，但还未正式接入旧主链完成替换验证。

- 今天已明确阶段 2 的剩余最小缺口在三刀之后进一步收敛，不再是大结构问题，而是细节补强问题。当前最小剩余缺口主要是：
  1. `waitForDreaminaCredentialFormReady(...)` 仍偏一次性判断，尚未补“等待型 ready 确认”；
  2. 阶段 2 成功后传给阶段 3 的交接载荷仍偏轻，目前核心只有 `nextStage: verification`，尚未形成更明确的 verification-ready 握手协议；
  3. Dreamina 阶段 2 failure reason 还未完全升级成旧链级业务语义，后续应基于真实日志再细化，而不是闭门造分类。
  后续若继续补阶段 2，默认优先先看真实运行日志，再决定是否补 ready 等待或交接载荷，不再盲目扩写逻辑。

- 今天已明确阶段 3 的正式边界定义：阶段 3 = **verification**。它的职责范围是：
  - 确认验证码阶段 ready
  - 获取验证码
  - 填写验证码
  - 提交验证码
  - 确认是否通过 verification 或为何失败
  阶段 3 的停止点：
  - 成功时：已确认进入下一阶段（如 birthday / onboarding / post-auth）
  - 失败时：已确认验证码错误、过期、未收到、被 challenge、无状态变化等原因
  阶段 3 明确不负责：首页、login gate、credential submit、birthday、session 等内容。

- 今天已明确阶段 3 相比旧 `task-register.js` 的核心缺口，不是某个小函数，而是整个新结构尚未工程化。最关键缺口包括：
  1. 还没有独立新包（未来应为 `shared-verification`）；
  2. 还没有 verification ready 判断器；
  3. 还没有验证码获取链的新抽象（邮件源 / code extract / 页面阶段协同）；
  4. 还没有验证码输入/提交策略层；
  5. 还没有 verification 成功出口 / 失败出口的确认与分类层。
  也就是说，阶段 3 目前只有边界定义和旧链经验，还没有像阶段 2 一样完成“新包 + adapter + orchestrator + profile + 日志文档 + 第一版真实行为”的落地。

- 今天已确认阶段 2 与阶段 3 的相对成熟度差距：
  - 阶段 2：已经有新包、代码、文档、日志模板和一轮补强，属于“可执行、可继续验证、可准备接主链”的状态；
  - 阶段 3：目前仍处于“边界已清楚、工程载体未落地”的前置阶段。
  后续推进顺序上，默认不要跳过阶段 2 的真实接链/验证就直接把阶段 3 做得过深；阶段 3 先应以最小新包骨架 + verification ready / result confirm 为第一落点，而不是一开始就把邮箱取码全链路铺满。
