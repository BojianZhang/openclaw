- 2026-04-12：`D:\playwright\Dreamina\Dreamina-register.js` 的 CLI 入口已补齐最小可执行闭环，并经过多轮实跑验证。当前已先后补上：Playwright `chromium` 启动壳（browser/context/page）、`--headed`/`--slow-mo`、默认有头调试、CLI 代理选择、`Dreamina/local-accounts.json` + `--account-index` 账号输入源、登录页资源拦截（image/media/font）、以及 entry/credential/verification 多处 Dreamina 专属桥接与诊断。后续若继续维护该 CLI，默认把它视为“可独立调试 Dreamina 注册七阶段主链”的专用入口，而不是空壳脚本。
- 2026-04-12：Dreamina 注册链本轮关键排查结论已明确：当前主页在不同代理/地区下会随机落到公开 marketing landing 页，而不是稳定自带登录入口；因此 entry 阶段不能只靠当前页 selector 硬找 `Sign in`，需要保留 recovery / reload / 多轮观察或更直接的 auth 入口策略。实际已通过 entry 失败快照确认过一类页面现场：只有 `Start Creating With AI Agent`、`AI Image/Video/Avatar`、`Try it now` 等公开内容，没有登录入口和输入框。
- 2026-04-12：Dreamina auth modal 本轮又确认一条高价值真实行为：验证码前的 credential submit 必须先区分当前弹窗是 `Sign in / Welcome back` 还是 `Sign up`。如果目标是注册而当前停在登录态，就必须先切到 `Sign up` 再填邮箱密码；否则即使成功点击 Continue，也会落成 `CREDENTIAL_SUBMIT_NO_STATE_CHANGE` 这类假问题。当前 `shared-credential/dreamina/credential-adapter.js` 已补“注册优先时自动切到 signup 模式”的前置策略。
- 2026-04-12：Dreamina CLI 当前默认调试模式应视为“有头 + 慢速 + 资源拦截”更可靠：用户明确要求后续调试默认使用 `--headed`，并通常配合 `--slow-mo 150/300` 观察真实页面；同时 CLI runtime 已补图片/媒体/字体资源拦截，用于加快登录页加载。后续在该链路上做首轮排查时，不要优先用纯无头模式。
- 2026-04-12：Dreamina verification 本轮已推进到真正验证码阶段，当前主链已能跑通到 `verification-submit`。现阶段第一真实阻塞不再是页面流转，而是 Firstmail provider 配置读取。明确结论：`D:\playwright\shared-verification\dreamina\profiles\dreamina-verification-profile.json` 现在承载 `mailProvider.firstmail.apiKey`；而底层 `D:\playwright\firstmail-api.js` 真正读取的是 `config.firstmailApiKey`（并兼容 env `FIRSTMAIL_API_KEY`）。因此 verification adapter 已补 profile apiKey 到 provider config 的字段映射；后续若再遇到 `FIRSTMAIL_API_KEY_MISSING`，默认优先检查 profile 中 apiKey 是否已填、以及 adapter 是否正确把它映射成 `firstmailApiKey` 传给 provider，而不是先怀疑验证码页或 Dreamina DOM。
- 2026-04-12：`D:\playwright\shared-verification\dreamina\verification-adapter.js` 已开始按新架构补 verification 收口能力：一是 `fillDreaminaVerificationCode(...)` 已补 attempts 级诊断，记录 hidden-input / wrapper-keyboard / fallback 三层输入尝试后的 `inputValue`、`boxTexts`、焦点元素与 stateChanged；二是 `confirmDreaminaVerificationSubmitResult(...)` 已提升“birthday 即 profile-completion 成功出口”的优先级，并新增 `transitionHint.enteredProfileCompletion` 交接信息；三是 `dreamina-verification-profile.json` 已补 birthday title texts（`When’s your birthday?` / `When's your birthday?`）与 `Next` 文本信号。后续验证重点应从“只看 fillResult 是否完美回读”切换到“是否已命中 profile-completion 成功出口并交棒给 shared-profile-completion”。
- 2026-04-12：`D:\playwright\shared-profile-completion\dreamina\profile-completion-adapter.js` 已启动 Month 专项修复：`buildDreaminaProfileCompletionPlan(...)` 的 `birthdayPlan.month` 已升级为带 `numeric / numericNoPad / shortName / fullName / candidates` 的多表示结构；`readDreaminaBirthdayMonthValue(...)` 已补 placeholder / textContent / ariaLabel / role / tagName / className / readMode / optionPanelVisible 等读取字段；`fillDreaminaBirthdayMonth(...)` 已从单一路径升级为“先 picker-select，再 direct-numeric / short-name / full-name”的多策略尝试，并沉淀 `attempts[]` 诊断。后续验证重点应直接看 `monthFillResult.mode`、`monthFillResult.attempts[]` 与 `profileCompletion.detail.monthFillResult.value`，确认 Month 到底是 picker 命中、名称格式命中，还是仍未真正应用。
- 2026-04-12：`D:\playwright\shared-verification\dreamina\verification-adapter.js` 已进入第二轮修复，方向从“继续堆 fill 策略”转为“先解决验证码控件激活失败”。当前已补：`resolveDreaminaVerificationInput(...)` 的 `interactionMeta`（visible/enabled/editable/boundingBox/role/tabindex 等），`readDreaminaVerificationInputState(...)` 的 active/wrapper/focus-cell 诊断字段，以及新的 `activateDreaminaVerificationInput(...)` 前置激活链（direct-input / wrapper / focus-cell 三层尝试）。`fillDreaminaVerificationCode(...)` 也已改为“先 activation，再 hidden-input / wrapper / fallback 输入”，并新增 `VERIFICATION_INPUT_ACTIVATION_FAILED` 站点失败语义。后续验证重点应优先看 `verification.detail.fillResult.activationResult` 与 attempts 里 `activeInsideVerificationWrapper` / `activeTag` 是否仍卡在 `BODY`，而不是先纠结验证码值本身。
- 2026-04-12：verification 已进入第三轮输入链修复：当前已新增 `tryDreaminaCharByCharInput(...)` 与 `ensureDreaminaVerificationInputAlive(...)`，把 Dreamina 验证码输入升级为“逐字符输入 + 每字符后状态采样 + 掉焦后即时 re-activate”的 OTP 组件专用路径，并将其提升为 `fillDreaminaVerificationCode(...)` 的第一优先策略。`fillResult` 现在会额外携带 `charSteps[]`，用于定位是第几个字符开始掉焦、是否短暂写入后又被清空、以及每个字符后 `activeTag / inputValue / boxTexts / activeInsideVerificationWrapper` 的变化。失败分类也开始区分 `DREAMINA_VERIFICATION_INPUT_BLURRED_DURING_FILL` 与 `DREAMINA_VERIFICATION_CODE_NOT_ACCEPTED_BY_COMPONENT`，后续应重点根据 `charSteps[]` 判断 Dreamina 组件究竟是“逐字符中途失焦”还是“完整事件链仍不认值”。
- 2026-04-12：根据生日页真实 DOM 现场，`shared-profile-completion/dreamina/profile-completion-adapter.js` 已进入第二轮结构修正：birthday plan 现已改为“随机完整合法生日（18+）”生成，输出 `isoDate` 与 `ageQualified`，不再独立乱拼 year/month/day；Year 已按真实 textbox 处理，并补“`Enter a four digit number` 错误提示是否消失 + Next 是否 enabled”的接受确认；Month 与 Day 已按真实 `li.lv-select-option` dropdown 模型处理，Month 只按英文全称（如 `November`）点选，Day 只按数字 option 点选，不再优先走普通 fill。后续验证重点应直接看 `birthdayFillPlan.isoDate`、`monthFillResult.attempts[0].panelVisible/optionMatched`、`dayFillResult.attempts[0].panelVisible/optionMatched` 与每步后的 `nextState.enabled`，确认页面是否真正接受了随机 18+ 生日。
- 2026-04-12：`shared-credential/dreamina/credential-adapter.js` 已开始针对 `CREDENTIAL_SUBMIT_NO_STATE_CHANGE` 做专项修复：一是 `ensureDreaminaSignupMode(...)` 已从“切一次 signup 就算完”升级为“切换后还要确认 `Sign up` 标题 / email+password 字段稳定存在”的模式确认；二是 `captureDreaminaCredentialSubmitSnapshot(...)` 已补 authTitle/authMode、Continue 是否 enabled、verificationVisible、inlineError、errorModal 等字段；三是 `submitDreaminaCredentialForm(...)` 已从单一路径 click 升级为 click / force-click / Enter 三策略 submit，并沉淀 `attempts[]` 记录每次 submit 后的 authMode、verificationVisible、inlineError 与 stateChanged；四是 `CREDENTIAL_SUBMIT_NO_STATE_CHANGE` 已开始细化为“所有 submit 策略后仍无状态变化”的语义。后续验证重点应看 `credential.detail.submitResult.attempts[]` 与 before/after snapshot 中的 `authMode/continueEnabled/verificationVisible`，判断是 signup 模式不稳定、Continue 未被页面接受，还是所有 submit 策略都未触发状态推进。
- 2026-04-12：阶段 2 的业务语义已进一步收敛：注册流程与登录流程不能再混用“signup-like”判断。当前 `shared-credential/dreamina/credential-adapter.js` 已开始按 intent 路由做结构重构，新增 `resolveDreaminaCredentialIntent(...)`、`clickDreaminaRegisterEntry(...)`、`confirmDreaminaRegisterMode(...)`、`confirmDreaminaSigninMode(...)`。当前 register 默认策略已改成“先显式点击 Sign up，再确认 signup 标题和字段稳定”，signin 则保留“不点 Sign up，直接确认 signin 模式”的逻辑分流。后续验证重点应先看 `formReady.intentResult` 与 `formReady.authModeResult` 是否一致，再看 register 流里的 submit 是否仍 no-state-change；如果仍失败，下一步更可能要继续修 email/password 的受控输入链，而不是再回到 authMode 切换层。
- 2026-04-12：Month 下拉已进入第三轮专项修复：`shared-profile-completion/dreamina/profile-completion-adapter.js` 现已新增 `readDreaminaBirthdayMonthDisplayState(...)`，不再只读 `input.value`，而会同时读取 trigger 附近 wrapper 的真实显示文本；`trySelectDreaminaBirthdayMonthOption(...)` 也已从“直接点 li”升级为优先点 option content / wrapper，并记录 `clickTarget`、`clickMode` 与 `panelStillVisible`。`fillDreaminaBirthdayMonth(...)` 当前会在点击 option 后执行两段 settle 读取，并同时判断 `panelClosed`、`displayMatched`、`effectiveValue`、`nextEnabled`。后续验证重点应直接看 `monthFillResult.monthApplyResult.panelClosed/displayMatched/effectiveValue`，用来区分是“面板没关”“触发器文本没更新”还是“文本更新了但页面仍未接受”。
- 2026-04-12：Month 第四轮与 Day 同步修复已开始：当前 `shared-profile-completion/dreamina/profile-completion-adapter.js` 已新增 `readDreaminaDropdownOptionSelectedState(...)`，把 birthday dropdown 成功标准从“点了 option + 面板关闭”升级为“目标 option 进入 selected 态 + trigger/field 值同步 + Next 变化”的组合判断。Month/Day 的 option click 现在改为 content-first，优先点 `.lv-select-option-content-text / .lv-select-option-content / span/div`，而不是优先点 `li` 本体；同时 Day 也同步进入同样的 selected-state 模型，避免 Month 修通后立刻在 Day 重复同类问题。后续验证重点应直接看 `monthFillResult.monthApplyResult.selected/displayMatched/effectiveValue` 与 `dayFillResult.attempts[0].selected/panelClosed/afterValue`，确认是“选项没进 selected”“selected 了但 trigger 没同步”，还是“字段同步了但 Next 仍未启用”。
- 2026-04-12：Month/Day 下拉点击链已继续收窄到“精确文本节点 + 最近可点击祖先”的模型：`trySelectDreaminaBirthdayMonthOption(...)` 现不再简单对命中的 option 做 first-content 点击，而是围绕目标文本构造 click plans（exact-text、其父节点、祖父节点、content-text、content、option），每次点击后立即复查 selected-state。当前后续验证应优先看 `clickTarget` 是否已从 `option` 变为 `exact-text* / content*`，以及 selected-state 是否终于从 false 变 true；如果仍长期停在 `selected=false + panelClosed=true`，说明 Dreamina 这个 dropdown 组件可能需要额外键盘确认或更精确的内部节点策略，而不是继续扩大外层表单逻辑。
- 2026-04-12：已开始把 `shared-profile-completion/dreamina/profile-completion-adapter.js` 的 Year/Month/Day 主路径向 `firstmail-dreamina-signup.js` 对齐：Year 现在优先走 `getByRole('textbox', { name: 'Year' })`，Month 优先走 `getByText('Month') + getByRole('option', { name: month })`，Day 优先走 `getByText('Day', { exact: true }) + getByRole('option', { name: day, exact: true })`；原先的 DOM selector / selected-state 精修逻辑已开始下沉为 fallback 与诊断层。后续验证重点应先看 `clickTarget` 是否出现 `role-option`，再看主路径是否直接把 Month/Day 推进成功；如果 role 路径稳定成功，后续应继续压缩 DOM 兜底逻辑，减少“自造一套组件推理链”带来的维护成本。
- 2026-04-12：birthday Month/Day 已继续进入“时序对齐修复”：当前主路径已按 `firstmail-dreamina-signup.js` 的节奏拉长为“打开 dropdown 后 ~850ms 再点 role-option，点后 ~900ms 再读状态；若仍未 selected/未回写，则 reopen 后再做一次 role-option 温和重试”，并把 `retried` 写入 attempts。后续验证重点应直接看 `monthFillResult.attempts[0].retried` / `dayFillResult.attempts[0].retried` 与 `selected` 是否从 false 变 true；如果 role 路径 + 慢节奏 + 一次重试后仍长期停在 `selected=false + panelClosed=true`，则高度怀疑需要额外键盘确认（如 Enter）或 Dreamina 在 shared 场景下存在更深的焦点/overlay 差异。
- 2026-04-12：birthday Month/Day 已进入 trigger-first 修复阶段：当前 `shared-profile-completion/dreamina/profile-completion-adapter.js` 新增 `readDreaminaBirthdayTriggerState(...)` 与 `activateDreaminaBirthdayTrigger(...)`，先围绕 `Month/Day` 文本、其父/祖父节点及 fallback selector 建立 opened/active/focused 的 trigger 激活链，再进入 role-option 选值。Month/Day 的 attempts 和 apply result 已开始补 `triggerActivated` 与 `triggerMode`。后续验证重点不再只是看 panel 是否打开，而要直接看 `triggerActivated=true/false`、`triggerMode` 与之后的 `selected` 是否联动变化；如果 trigger 已激活但 selected 仍长期为 false，则下一步才值得考虑键盘确认或更深层 overlay/focus 差异。
- 2026-04-12：已开始把 `firstmail-dreamina-signup.js` 的 birthday 成功交互原样适配进 shared-profile-completion 主路径，而不是继续自造一套组件推理链：Year 现优先按 `getByRole('textbox', { name: 'Year' }) -> click -> fill -> settle`；Month 现优先按 `getByText('Month') -> wait ~900ms -> getByRole('option', { name: month }) -> wait ~1100ms`；Day 同理按 `getByText('Day', { exact: true }) -> role option`。当前这些 reference-flow 已上升为主路径，原 trigger/DOM/selected-state 逻辑开始退为 fallback 与诊断层。后续验证重点应先看 `mode` 是否变成 `reference-flow`、`clickTarget` 是否稳定为 `role-option`，以及字段是否脱离 placeholder 与 `Next` 是否终于变 enabled；如果 reference-flow 仍不工作，才说明 shared 场景与单文件脚本的页面上下文确实存在更深层差异。
