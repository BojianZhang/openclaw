# 2026-04-13

- 对 `D:\playwright\Dreamina` 与 `shared-account-delivery` / `shared-credential` / `shared-entry` / `shared-post-auth-ready` / `shared-profile-completion` / `shared-proxy-precheck` / `shared-verification` 做了整套 staged architecture 静态审计。
- 本轮审计确认的关键结论：当前整体不是推倒重来问题，而是进入“主路径冻结后，代码与文档仍停留在过渡桥接期”的错位状态。
- 明确查出 1 个代码级硬伤：`shared-profile-completion/stages/profile-completion-submit.js` 中使用了未定义变量 `hasSplitFlow` / `hasContinuousFlow`，存在运行时 `ReferenceError` 风险，应优先修复。
- 第 4 阶段（profile-completion）当前长期主路径仍应冻结为 `fillDreaminaBirthdayContinuousFlow`；split fill 逻辑最多保留为 fallback / diagnostics，不应继续伪装为并列主路径。
- 第 5 阶段（post-auth-ready）当前实现与文档存在明显错位：README/边界文档定义其应接住“离开 profile-completion 后的已登录态/用户态确认”，但 Dreamina profile 与 adapter 仍在大量使用 birthday 页元素（如 `Year/Month/Day/Next`、birthday title/subtitle）作为 ready/bridge 信号。当前更准确的定位应是“bridge-only/transition-only 过渡阶段”，而非真正成熟的 post-auth-ready 主阶段。
- 第 6 阶段（account-delivery）当前也仍偏 bridge/minimal payload 模式：Dreamina profile 中 `requiredFields` 只有 `email`，`successSignals/failureSignals` 为空，`deliveryReady` 主要靠 URL/account context 弱信号触发；这与 README 中“registration-complete 之后的强交付确认阶段”描述不一致。当前更准确定位应是最小交付对象整理层，而非强交付确认层。
- 验证码阶段（shared-verification）当前主路径与长期决策基本一致：`dreamina-direct-fill` 仍是主路径；`char-by-char` / `hidden-input` / `wrapper-keyboard` / `fallback-keyboard-type` 仍可保留，但只应视为 debug/diagnostics/legacy fallback，不应自动回到主链。
- 目前未发现典型无限死循环主链；entry / verification 的重试均有次数边界。但 verification 仍可能出现“有限次数内的低收益重复尝试”，后续可继续细分 stale code / provider delay / resend 后邮箱未推进等原因。
- 文档与文件健康方面也确认了两类问题：一是多份 README 仍在写目标态，没有如实标注当前 bridge 状态；二是多个 Dreamina profile 中存在 `When��s` / `won��t` 一类乱码文本，属于文件健康和规则污染问题，应顺手清理。
- 下一步计划：优先修复第 4 阶段未定义变量硬伤，并同步收口第 5/6 阶段 README、边界描述与 Dreamina profile 的 bridge 标识，减少“文档已毕业、代码仍桥接”的认知错位。
- 2026-04-13：用户明确重新划定 `D:\playwright` 的架构边界，后续必须按此长期认知执行，不能再混说。老架构边界为：`D:\playwright\config.json`、`D:\playwright\dreamina-health.js`、`D:\playwright\dreamina-register-profile-loader.js`、`D:\playwright\dreamina-register-profile.json`、`D:\playwright\firstmail-api.js`、`D:\playwright\proxies.txt`、`D:\playwright\proxy-precheck.js`、`D:\playwright\runner.js`、`D:\playwright\task-register.js`、`D:\playwright\results`；新架构边界为：`D:\playwright\Dreamina`、`D:\playwright\shared-account-delivery`、`D:\playwright\shared-credential`、`D:\playwright\shared-entry`、`D:\playwright\shared-post-auth-ready`、`D:\playwright\shared-profile-completion`、`D:\playwright\shared-proxy-precheck`、`D:\playwright\shared-verification`。后续讨论新系统运行、staged architecture、阶段编排、日志体系、阶段耗时、结果交付、边界整改与演进时，默认优先站在新架构边界上说；不要再默认回到老 `runner.js` / `task-register.js` 语境。
- 2026-04-13：`D:\playwright\Dreamina\Dreamina-register.js` 作为新架构单账号正式 CLI 入口已完成一轮实战可用性提升：控制台输出已改成“摘要 + 文件路径”，完整 result JSON 改为落盘到 `D:\playwright\Dreamina\results`；结果目录已按 `success/failed/latest` 分桶，并新增 per-account latest、overall latest、recent index（`dreamina-cli-index.json`）与 compact `stageSummary`；CLI 摘要已补齐 `StageSummary`、`TimingSummary`、`SlowestStage`、失败时的 `FailureStage/FailureReason` 高亮。后续单账号调试默认优先使用该新入口，而不是老架构入口。
- 2026-04-13：新架构单账号实跑结论已经拿到两个关键样本并应视为长期事实：1）`proxy-index 0` 曾命中 `PROXY_CONNECT_HTTP_407`，说明第 0 阶段 `shared-proxy-precheck` 已能正确在 CONNECT 认证失败时拦停并将代理标记为 bad；2）`proxy-index 1` 已成功跑通到 `account-delivery`，证明新 staged architecture 主链可完整执行；另有同代理下账号存在样本会在 `credential-submit` 正确命中 `ACCOUNT_ALREADY_EXISTS` 并失败收口。后续默认把这些视为“新架构入口、阶段日志、阶段耗时、失败分类、结果落盘均已基本成型”的实证依据。
- 2026-04-13：基于新架构实跑结果，再次验证此前审计判断：前 0~4 阶段（proxy-precheck、entry、credential、verification、profile-completion）已经具备真实通路；其中 verification 的 `dreamina-direct-fill` 主路径与 profile-completion 的 `fillDreaminaBirthdayContinuousFlow` 主路径都已实跑证明可用。当前真正还未成熟的是第 5/6 阶段：`post-auth-ready` 仍停留在 `POST_AUTH_READY_ONLY` 这类 bridge/transition 成功语义，`account-delivery` 仍以 `DELIVERY_PAYLOAD_READY` / `SESSION_SIGNAL_NOT_FOUND` 这类 minimal delivery / weak confirmation 方式收口。后续 Dreamina staged architecture 的主整改重点应继续放在第 5/6 阶段强 session / 强 delivery confirm 上，而不是重新怀疑前四阶段是否能跑通。
- 2026-04-13：已新增 `D:\playwright\Dreamina\Dreamina-batch-runner.js`，这是新架构批量运行入口的最小 runnable 雏形，语义上明确属于新架构而不是老 `runner.js` 的延伸。当前版本已具备：CLI 参数解析、账号队列、简单代理轮询分配、worker loop、调用 `runDreaminaRegisterFlow(...)` 执行单账号新主链、batch overview 输出、batch summary / latest / index 落盘。第一版边界也已明确：只负责新架构批量调度骨架，不复制单账号业务主链，不在此阶段引入复杂 retry / 智能代理淘汰 / GUI 看板 / 新 config 体系。后续如要推动老 `runner.js` 退休，优先应继续沿这份 `Dreamina-batch-runner.js` 演进，而不是回头增强老批量入口。
- 2026-04-13：`Dreamina-batch-runner.js` 首轮实测前已暴露并修复一个具体实现坑：`loadLocalProxies` 不能从 `Dreamina-register.js` 获取，正确来源是 `D:\playwright\shared-proxy-precheck\local-proxy-loader`。后续只要继续扩展新 batch runner，默认先警惕“单账号 CLI 文件导出边界”和“shared loader 实际来源”这类 wiring 问题，避免误把 shared 能力假定为已由 `Dreamina-register.js` 重新导出。
- 2026-04-13：旧 OpenClaw 目录 `D:\260413 OpenClaw\.openclaw` 已完成增强迁移盘点与收口。已确认最有价值的可迁移资产是旧 workspace 下的 `IDENTITY.md`、`USER.md`、`TOOLS.md`、`MEMORY.md`、`memory/*` 以及一批自定义 skill；`openclaw.json`、`identity/device*.json`、`devices/`、`logs/`、`completions/`、sqlite memory DB 等运行态/鉴权/设备绑定资产不建议直接覆盖迁移。后续若再做旧 `.openclaw` 迁移，默认采用“记忆/身份/工具/自定义技能优先，配置与设备态谨慎人工比对”的策略。
- 2026-04-13：旧 `.openclaw` 的 skills 已完成一轮分级收口。高价值候选区包括 `proxy-automation-stabilizer` 与整套 Java 技能树（`java-knowledge-corpus`、`java-troubleshooting-triage`、`java-learning-planner`、`java-interview-answering`、`java-concurrency-jvm`、`java-data-backend`、`java-design-refactor`、`java-jvm-performance`、`java-network-microservices`、`java-spring-microservice`）；参考库保留 `frontend-template-corpus`、`html-template-refactor`、`frontend-slides`、`skill-vetter`、`summarize`；`self-improving-agent` 已明确归档观察，不建议当前启用。后续若继续收口技能，默认把 `proxy-automation-stabilizer` 和 Java 技能树视为最优先转正候选。
- 2026-04-13：OpenClaw 环境健康检查已确认当前无大故障，但 memory search 语义检索能力未真正恢复。`openclaw doctor --non-interactive` 给出的核心结论是“Memory search is enabled, but no embedding provider is ready”，说明 markdown 记忆文件虽已迁回，但 embedding provider / model 配置仍未就绪。后续若要恢复 `memory_search` 体验，默认优先检查 `openclaw memory status --deep`、API key 或 `openclaw configure --section model`，而不是误以为记忆文件迁回就等于检索恢复。
- 2026-04-13：`D:\playwright\Dreamina\Dreamina-batch-runner.js` 本轮已完成一个关键业务顺序修复，并经实跑验证通过：本地已知 exists 账号现在会在“取代理/代理预检”之前直接跳过，不再浪费代理和预检时间；对应开发态还新增了 `skipCredentialExistsPrecheckAfterEmail` 运行开关，使非 exists 账号在 `credential-submit` 阶段完成邮箱填写后可直接进入密码填写，不再在本地开发场景下执行 email 后 exists precheck。验证结果已确认：已知 exists 账号前置 skip 生效，非 exists 账号可按“邮箱 -> 密码 -> 提交 -> verification -> profile-completion -> post-auth-ready -> account-delivery”完整跑通。相关修复已提交到 `D:\playwright`，commit 为 `c2ac429`。
- 2026-04-13：Dreamina 新架构与老架构对比的当前阶段判断进一步明确。老架构（`runner.js`、`task-register.js`、`proxy-precheck.js`、`config.json`、`firstmail-api.js`、`results` 等）应继续视为旧资产/旧运行体系；新架构（`Dreamina/` 与各 `shared-*` stage 包）已经从“概念架构”成长为“可实跑、可定位、可收口”的正式主干。当前前半段 `proxy-precheck -> entry -> credential -> verification -> profile-completion` 已具备真实主链、阶段日志、阶段耗时、失败分类、结果落盘与 batch 入口；但第 5/6 阶段仍处于 bridge/minimal-delivery 期，属于“前半段已成型，后半段仍在桥接收口”的状态。后续默认不再回头增强老 `runner.js`，而是优先继续增强新 batch runner 和第 5/6 阶段强确认能力。
- 2026-04-13：`shared-post-auth-ready` 本轮静态审计结论已明确：公共骨架本身是健康的，但 Dreamina 第 5 阶段真正缺的不是流程，而是“证据质量”。`dreamina-post-auth-ready-profile.json` 已明写 `mode=bridge`，且当前 `sessionSignals`、`successSignals`、`failureSignals` 基本为空，`postAuthReady` / `uiSignals` 仍大量依赖 birthday bridge 信号（birthday title/subtitle/next、Year/Month/Day/Next）。因此第 5 阶段现在虽然能返回成功，但本质上仍是 bridge 成功，典型状态是 `POST_AUTH_READY_ONLY`。后续整改优先级应默认是：先通过成功样本取证补实 `sessionSignals`（cookies/localStorage/sessionStorage 稳定 key）与 `successSignals`（真实登录后 UI / workspace / user panel 强信号），再把 bridge 信号与真实 post-auth 信号分层得更诚实，而不是继续堆流程代码。
- 2026-04-13：`shared-account-delivery` 本轮静态审计结论已明确：第 6 阶段当前本质上是“minimal payload / bridge layer”，不是独立强确认层。`dreamina-account-delivery-profile.json` 已明写 `mode=minimal-delivery`，且 `deliveryReady`、`summarySignals.sessionKeys`、`successSignals`、`failureSignals` 基本为空。当前成功主要依赖桥接路径：`accountSummary.ok && deliveryPayload.ok => DELIVERY_PAYLOAD_READY`。更关键的是，第 6 阶段会显式吃第 5 阶段 detail 里的 `sessionInspection`、`uiConfirmation`、`postAuthResultConfirmation`，并把 `postAuthResultConfirmation.state` 写入 delivery payload 的 `registrationState`。因此当前 5→6 的关系应默认理解为“第 6 阶段不是在纠正第 5 阶段，而是在继承并封装第 5 阶段结果”；若第 5 阶段仍停在 `POST_AUTH_READY_ONLY` 这类弱态，第 6 阶段也只能产出 `DELIVERY_PAYLOAD_READY` 这类最小交付成功。后续整改顺序默认应是：先补强第 5 阶段证据质量，再补第 6 阶段的 `summarySignals.sessionKeys`、`successSignals`、`deliveryReady.selectors`，最后再收紧 minimal-delivery bridge success 条件。
- 2026-04-13：对 Dreamina 老架构（重点 `D:\playwright\task-register.js`、`D:\playwright\runner.js`）与新 staged architecture 做进一步对比后，已确认老架构最值得继承的不是“大函数串行流程”，而是“成功必须有硬证据”的工程经验。当前最有价值的三条可复用经验是：① session 只认 Dreamina/CapCut 域下的 `sessionid` cookie，且老架构会显式等待 `sessionid` 出现，未命中则持续记录 interesting cookies 并最终以 `SESSIONID_COOKIE_NOT_FOUND` 失败，不会拿宽泛 storage/cookie 乱判成功；② 注册完成后会显式等待 `postRegisterReady` 的真实主页/UI ready 信号（selectors/texts），而不是仅凭 URL 或流程走到某步就算成功；③ 成功交付时不仅组装对象，还会实际落地 storage state、sessionid、国家+sessionid 等可交付结果，所以其“成功”更接近业务可用成功而不是仅 payload 成功。
- 2026-04-13：基于老架构对比，Dreamina 新架构第 5/6 阶段的默认补强方向进一步收敛：`shared-post-auth-ready` 后续优先把 `sessionSignals.cookieKeys` 先补成以 `sessionid` 为核心，并增加 Dreamina/CapCut 域约束；`successSignals` 应优先继承老架构 `postRegisterReady` 那类真实主页 / workspace / home ready selectors/texts，而不是继续主要依赖 birthday bridge 信号。随后 `shared-account-delivery` 再同步补 `summarySignals.sessionKeys`、`deliveryReady`、`successSignals`，并把第 6 阶段的成功逐步从“payload ready”提升为“payload + session deliverable ready / storage deliverable ready”。后续若继续推进 5→6 强确认，应默认沿着“sessionid + 真实主页 ready + 可交付 storage/session 结果”这条线收口，而不是继续扩写宽泛桥接条件。

- 2026-04-13 19:31 +08: Dreamina batch runner 进一步排查确认一条关键长期经验：`D:\playwright\Dreamina\Dreamina-batch-runner.js` 中 `knownExistsAccounts` 之前只会在 `ACCOUNT_ALREADY_EXISTS` / `DREAMINA_ACCOUNT_ALREADY_EXISTS` 等 existsFailure 分支写入，导致“本次已注册成功”的账号不会进入 `dreamina-known-exists.json`，下次重跑时仍会重复消耗代理预检与注册链资源。已在 `updateBatchSummary(...)` 中补上 `result.success === true` 时也将标准化 email 写入 `batchContext.knownExistsAccounts` 的逻辑；后续默认应把“已明确成功注册的账号”也视为已知已注册账号，在下一轮批跑中于代理预检前直接跳过，而不是只把 exists-failure 当作唯一的已注册信号。

- 2026-04-13 晚：Dreamina 新一轮批跑 `dreamina-batch-2026-04-13T14-05-41-178Z` 进一步确认当前主失败面已从 entry 切换到代理治理。该轮结果为 `successCount=3 | failedCount=5 | skippedCount=27`，失败桶仅剩 `DREAMINA_PROXY_CONNECTIVITY_FAILED=5`，且全部在 S0 `proxy-precheck` 的 `http-connect` 阶段于约 400~500ms 内快速失败，说明更像代理 CONNECT/认证/会话失效，而不是 Dreamina 页面、entry 识别或验证码链路问题。后续优先级应调整为：P0 代理治理，P1 S5/S6 强确认，entry 降级为观察项。
- 2026-04-13 晚：同一轮批跑也给出一个关键正向结论，先前反复出现 entry 失败的账号 `matthewsanchez1963@sungeingor.com`、`ednascott1911@vladoulead.com`、`jimmywood1943@wiwsjambok.com` 均已成功跑通到 `DELIVERY_COMPLETE`，说明 entry 阶段当前更像分支/环境抖动，而非稳定性主矛盾；阶段性判断可更新为“entry 暂时可用，继续观察，不再作为当前 P0 排查目标”。
- 2026-04-13 晚：S5/S6 强确认整改进入下一步，方向明确为“只收紧成功判定，不动主链编排”。新增长期规则：Dreamina 的 `post-auth-ready` / `account-delivery` 后续默认不再接受“任意文本命中即宣布成功”的宽松策略，文本型成功必须同时具备更强佐证（如 session 信号、非 bridge selector/UI、或来自上一阶段的更强已确认状态），并优先排除 birthday bridge 信号与 `Avatar` 这类高噪声风险词的单独误判。
- 2026-04-13 晚：S5/S6 收紧过程中又暴露一条实现层经验：条件表达式很容易写成“看似区分 reliable/unreliable text，实际上逻辑等价为未区分”的伪分支。后续继续收紧 `shared-post-auth-ready` / `shared-account-delivery` 时，默认要把规则拆成明确的分级判断（如 `Avatar` 必须带强佐证、`Canvas/Workspace/Home` 分级对待），不要堆复杂布尔表达式自我欺骗；必要时优先重构为命名良好的中间布尔变量，再决定是否放行 `REGISTRATION_COMPLETE` / `DELIVERY_COMPLETE`。

- 2026-04-13 深夜继续排查 Dreamina staged architecture 时，确认 `D:\playwright\shared-post-auth-ready\dreamina\post-auth-ready-adapter.js` 已完成一轮 UI 主状态优先级重排，并已提交 `56a19e9`（`refactor(dreamina): prefer workspace ui over bridge ui`）。核心长期结论：`confirmPostAuthUi(...)` 必须先判 `WORKSPACE_UI_SELECTOR_VISIBLE`、再判 `WORKSPACE_UI_TEXT_VISIBLE`，只有 workspace 未命中时才回退到 `BRIDGE_UI_SELECTOR_VISIBLE` / `BRIDGE_UI_TEXT_VISIBLE`，否则 birthday/bridge 残留元素会抢占登录后主工作台状态，继续污染 S5 的 UI 语义。
- 2026-04-13 又确认一个必须记住的 Dreamina 失败证据链路问题：`shared-credential/stages/credential-submit.js` 在 `CREDENTIAL_SUBMIT_RESULT_UNKNOWN` 时其实已经产出完整 stage detail（含 `formReady/emailResult/passwordResult/submitResult/confirmResult/classified`），但 `D:\playwright\Dreamina\Dreamina-register.js` 的主链失败收口此前没有把当前失败阶段的 `stageResult.detail` 提升到顶层 `result.detail`，导致 batch failed record 落盘时出现 `detail: null`，无法分析 credential 提交后真实页面分支。后续凡是 staged architecture 某阶段 failed json 出现 detail 缺失，不要先怀疑 stage 本身，默认先检查“主链 normalize / failure settlement 是否把当前 stage detail 透传到顶层 result”。
- 2026-04-13 已对上述 credential failure 证据链问题落地修复，并提交 `9727827`（`fix(dreamina): persist credential failure detail in flow result`）。修复点在 `D:\playwright\Dreamina\Dreamina-register.js`：`normalizeDreaminaRegisterResult(...)` 新增顶层 `detail` 字段，主链失败返回时显式带上 `detail: stageResult?.detail || null`。这条修复的长期意义是，后续再出现 `DREAMINA_CREDENTIAL_SUBMIT_RESULT_UNKNOWN`，对应 failed json 应能直接看到提交后 URL / bodyPreview / matchedTexts / matchedSelectors 等证据，不再陷入“控制台知道失败，但落盘文件是空壳”的盲排状态。
