# 2026-04-14

- Dreamina S3 `VERIFICATION_CODE_FETCH_FAILED` 本轮已形成更强阶段性结论：`coralfarmer1940@snhlsoble.com` 在旧 artifact 中失败，但在放宽预算后的新一轮直接以 `VERIFICATION_CODE_FETCHED` 成功，且 `stepDurationMs=2148ms`，从而坐实旧失败根因是旧预算链 timeout，而不是账号本身坏掉，也不是验证码匹配策略先天漏判。
- 截至本轮，Dreamina S3 新预算有效性的连续证据已明确累积：`marymoore1961@wahpanjim.com` 旧失败后新成功，`coralfarmer1940@snhlsoble.com` 旧失败后新成功，`annjohnson1951@satinonl.com` 在 `14267ms` 成功拉码，`catherinegarner1906@snhlsoble.com` 在 `34614ms` 成功拉码。长期判断应固定为：旧约 13 秒预算明显偏紧，S3 存在真实长尾，放宽预算是打中了主矛盾，而非偶然波动。
- Dreamina staged architecture 截至本轮的阶段性健康判断应更新为：S1 entry 已明显稳定，S2 credential 暂不再是主矛盾，S3 verification 的旧预算问题已被有效缓解，S4/S5/S6 当前可持续稳定通过；系统整体已进入“主链高成功率，剩余问题主要是慢验证码长尾治理”的阶段。
- 后续 Dreamina S3 的默认策略应从“大改修主链”切换为“观察 + 小修”：继续跑批，仅对“新预算下仍失败”的 `VERIFICATION_CODE_FETCH_FAILED` 新 artifact 做针对性分析，再决定是否需要继续上更长预算、前快后慢 polling、resend-aware 策略，或 `recent-primary` / 匹配增强。不要在当前成功率明显提升后立刻再次做大范围策略改写。

- 2026-04-14 03:41 CST：Dreamina 硬 session 提升链路已完成真实闭环验证。单账号批次 `dreamina-batch-2026-04-13T19-32-17-515Z` 中，`jameslevy1958@ttattdo.com` 的 `sessionObservationTrace` 提升结果已进入 canonical/effective session，`batch-results/latest/dreamina-batch-latest.json`、`deliveryPayload.sessionSummary`、`registered-accounts.json`、`session-records/latest.jsonl` 均已写入真实 `sessionId=1ec34f09106c437c27f537e2fbf839a3`，并标记 `hasHardSession=true`、`source=cookie`。这说明此前“late-arriving sessionid 只观测不消费”的主 bug 已被修通。
- 同轮验证还确认一个新的明确结论：`session-records/latest.txt` 中出现 `邮箱------------sessionId` 不是 session 提取失败，而是导出格式固定为 `邮箱----国家代码----国家名称----sessionId`，但当前批次 `countryCode/countryName` 没有上游来源，导致中间两段为空。后续再看到这种表现时，默认先判断为“国家字段缺来源”，不要误判为 hard session 链路回退。
- 当前 Dreamina 会话资产化状态已进入下一阶段：主矛盾已从“sessionid 提升与写回”切换为“countryCode/countryName 如何从代理/批次/account 上下文正式灌入 delivery 与 session-records 导出链路”。后续若继续完善导出文本质量，优先补代理国家信息传播，不必回头重写 session 提取主链。
- 2026-04-14 04:00 CST 后，Dreamina staged architecture 已验证一轮低风险性能压缩有效：`shared-entry/dreamina/entry-adapter.js` 的 fallback staged wait 从 `8/10/10s` 缩到 `6/6/4s`，`shared-post-auth-ready/stages/post-auth-ready.js` 的 session observation 改为“首次命中 hard sessionid 即提前收口，不再继续跑 3s/5s 观察尾巴”。验证批次 `dreamina-batch-2026-04-13T20-05-23-551Z` 成功跑通，主链总耗时从前一轮约 `58952ms` 降到 `50530ms`，其中 `post-auth-ready` 从约 `5223ms` 明显降到 `711ms`。长期结论应固定为：当前阶段最值钱的性能优化，不是重写主链，而是优先清理成功路径上的保守等待尾巴。
# 2026-04-14

- Dreamina S3 `VERIFICATION_CODE_FETCH_FAILED` 本轮已形成更强阶段性结论：`coralfarmer1940@snhlsoble.com` 在旧 artifact 中失败，但在放宽预算后的新一轮直接以 `VERIFICATION_CODE_FETCHED` 成功，且 `stepDurationMs=2148ms`，从而坐实旧失败根因是旧预算链 timeout，而不是账号本身坏掉，也不是验证码匹配策略先天漏判。
- 截至本轮，Dreamina S3 新预算有效性的连续证据已明确累积：`marymoore1961@wahpanjim.com` 旧失败后新成功，`coralfarmer1940@snhlsoble.com` 旧失败后新成功，`annjohnson1951@satinonl.com` 在 `14267ms` 成功拉码，`catherinegarner1906@snhlsoble.com` 在 `34614ms` 成功拉码。长期判断应固定为：旧约 13 秒预算明显偏紧，S3 存在真实长尾，放宽预算是打中了主矛盾，而非偶然波动。
- Dreamina staged architecture 截至本轮的阶段性健康判断应更新为：S1 entry 已明显稳定，S2 credential 暂不再是主矛盾，S3 verification 的旧预算问题已被有效缓解，S4/S5/S6 当前可持续稳定通过；系统整体已进入“主链高成功率，剩余问题主要是慢验证码长尾治理”的阶段。
- 后续 Dreamina S3 的默认策略应从“大改修主链”切换为“观察 + 小修”：继续跑批，仅对“新预算下仍失败”的 `VERIFICATION_CODE_FETCH_FAILED` 新 artifact 做针对性分析，再决定是否需要继续上更长预算、前快后慢 polling、resend-aware 策略，或 `recent-primary` / 匹配增强。不要在当前成功率明显提升后立刻再次做大范围策略改写。

- 2026-04-14 03:41 CST：Dreamina 硬 session 提升链路已完成真实闭环验证。单账号批次 `dreamina-batch-2026-04-13T19-32-17-515Z` 中，`jameslevy1958@ttattdo.com` 的 `sessionObservationTrace` 提升结果已进入 canonical/effective session，`batch-results/latest/dreamina-batch-latest.json`、`deliveryPayload.sessionSummary`、`registered-accounts.json`、`session-records/latest.jsonl` 均已写入真实 `sessionId=1ec34f09106c437c27f537e2fbf839a3`，并标记 `hasHardSession=true`、`source=cookie`。这说明此前“late-arriving sessionid 只观测不消费”的主 bug 已被修通。
- 同轮验证还确认一个新的明确结论：`session-records/latest.txt` 中出现 `邮箱------------sessionId` 不是 session 提取失败，而是导出格式固定为 `邮箱----国家代码----国家名称----sessionId`，但当前批次 `countryCode/countryName` 没有上游来源，导致中间两段为空。后续再看到这种表现时，默认先判断为“国家字段缺来源”，不要误判为 hard session 链路回退。
- 当前 Dreamina 会话资产化状态已进入下一阶段：主矛盾已从“sessionid 提升与写回”切换为“countryCode/countryName 如何从代理/批次/account 上下文正式灌入 delivery 与 session-records 导出链路”。后续若继续完善导出文本质量，优先补代理国家信息传播，不必回头重写 session 提取主链。
- 2026-04-14：Dreamina 本轮低风险性能优化已验证有效并应视为默认保留：`shared-entry/dreamina/entry-adapter.js` 已将 entry fallback staged wait 从 `8/10/10s` 收缩到 `6/6/4s`；`shared-post-auth-ready/stages/post-auth-ready.js` 已在首次命中 hard `sessionid` 后提前结束 observation，不再强制跑完后续 `3000/5000ms` 观察尾巴。连续样本表明 S5 `post-auth-ready` 已从原先约 5.2s 稳定降到约 0.6s~1.9s，总链路多数收敛到约 47s~51s，说明这轮收益真实且稳定，后续不要回退到旧的保守尾等待。
- 2026-04-14：Dreamina `session-records` 的语义在本轮已重新定准。`dreamina-session-batch-*.txt/jsonl` 继续承担历史批次归档；`session-records/latest.txt` 与 `latest.jsonl` 不再表示“全历史累计”，而改为“当前批次累计结果”。每次启动新的 `Dreamina-batch-runner` 时，应自动先清空这两份 latest 文件，然后仅累加本批次成功提取到的 session；同一批次内仍需做去重追加，避免重复 session 污染 latest。
- 2026-04-14：`Dreamina/Dreamina-batch-runner.js` 里的 `appendFirstSessionRecord(...)` 已修正过两轮关键问题。第一轮修复了 latest 文件错误使用 `writeFile` 导致只保留最后一条的问题；第二轮又按用户真实需求改成“批次开始自动清空 latest，再按本批次去重累计”。后续如果再改 session-records，不要把 `latest.*` 当作永久总账，而应明确它只是 current batch 视图。
- 2026-04-14：Dreamina `shared-proxy-precheck` 的本轮性能优化已验证有效。原实现中 `checkProxyConnectivity(...)` 与 `checkDreaminaPrimaryTarget(...)` 都会重复请求 `https://www.dreamina.com/`，且 `exitIp -> primary -> secondary` 三项检查是串行执行，导致 `proxy-precheck` 容易出现 11s~23s 的波动长尾。本轮已将 connectivity 改为走 `exitIp` 轻目标，并把 `exitIp / primary / secondary` 改为并行检查。验证批次 `dreamina-batch-2026-04-13T20-37-58-533Z` 中，两次 `proxy-precheck` 已降到 `9772ms` 与 `6145ms`，对应总链路也压到 `44626ms` 与 `41698ms`，且 `successCount=2`。长期结论：当前预检优化方向应优先遵循“去重真实目标探测 + 可并行的网络探测并行化”，不要再保留重复 Dreamina 首页请求的串行成本。
# 2026-04-14

- 2026-04-14：Dreamina `shared-proxy-precheck` 的并行化优化在后续验证批次中再次稳定成立。用户贴出的批次 `dreamina-batch-2026-04-13T20-42-10-851Z` 中，两次 `proxy-precheck` 分别收敛到 `6061ms` 与 `6087ms`，总链路对应为 `45658ms` 与 `40301ms`，说明此前把 `exitIp / primary / secondary` 并行、去掉重复 Dreamina 首页探测之后，S0 已从波动主瓶颈明显降级为相对稳定阶段。后续若继续做性能工作，默认不再优先盯 S0，而应把焦点转向更顽固的固定时延阶段。
- 2026-04-14：Dreamina `shared-entry` 的最新多批次验证已形成新的长期判断。即使 `dreamina-entry-profile.json` 的 `loginSignalStages` 从旧的 `8/10/10s` 收到 `6/6/4s`，S1 `entry` 仍持续稳定落在约 `17.5s~17.8s`，例如 `17764ms`、`17645ms`、`17808ms`、`17538ms`。这说明当前 S1 的主要耗时不在 staged wait 配置本身，而更可能在 `openEntryPage` 后页面真实渲染 email gate / auth UI 的固定前置链路。后续处理 entry 性能时，默认应优先做细粒度打点与页面前置链路分析，而不是继续机械压缩 `loginSignalStages`。
- 2026-04-14：Dreamina staged architecture 本轮系统审计已形成新的文档/契约层长期判断：代码能力已经明显领先于文档契约能力，当前状态可概括为“主链代码已具备运行、批跑、调优与闭环能力，但阶段说明、字段语义、detail 稳定边界尚未完全追平”。尤其需要长期记住的高优先级收口点包括：S4 `profile-completion-submit` 的说明仍未完全对齐“continuous-flow 为主路径冻结、split-flow 为 fallback/bridge”的真实决策；S5 `post-auth-ready` 的 `initialSessionInspection / sessionObservationTrace / sessionInspection` 三层关系尚未被正式写死；S6 `accountSummary / deliveryPayload` 的稳定交付语义仍需明确。后续只要继续推进新架构，不应只改代码，还应同步推进阶段契约文档与字段语义收口。
- 2026-04-14：关于新架构的长期产品与开源定位，本轮已形成清晰判断：Dreamina staged architecture 的核心价值不应被包装成“某站自动注册工具”，而应定位为“可观测、可维护、可持续优化的复杂网页自动化运行时 / staged automation runtime”。若后续考虑开源，默认不应原样公开 Dreamina 实战链路，而应按“框架层开源、站点策略层私有”切层，优先开源 staged automation core、统一结果契约、批量运行骨架、artifact 模型与 demo adapter，而把 Dreamina/邮箱/provider/代理等强实战策略层继续留在私有域。
- 2026-04-14：关于 AI 结合路径，本轮已形成长期策略结论：这套架构最适合的不是纯聊天 AI，也不是完全自由点击的浏览器 Agent，而是“诊断型 AI + 调优型 AI + 编排型 AI + 代码协作型 AI”共同消费 staged artifacts 和阶段契约。后续若给框架接“大脑”，默认应坚持“Brain 做指挥/诊断/策略层，Runtime 做执行/状态/结果层”的双层结构，不要让上层 AI 直接绕开 staged architecture 退回黑盒自由点击模式。更长期的方向可理解为“领域定制化 + RAG + 策略层”的自动化协同系统，而不是普通问答式 RAG。
- 2026-04-14：关于 OpenClaw / Claude 的关系，本轮已形成一个值得长期保留的产品判断：这套系统与 OpenClaw 并非同层正面替代，更适合被定位为“复杂网页自动化执行引擎 / automation runtime”，而把 OpenClaw、Claude 这类系统视为上层大脑、会话编排或通用 Agent 平台。后续若做集成，推荐的默认边界是：OpenClaw/Claude 负责任务理解、对话、策略判断与多工具编排，这套 staged runtime 负责浏览器自动化执行、结构化结果输出、失败诊断表面与可调优运行时。这个边界应视为当前最健康的发展方向。
# 2026-04-14

- 2026-04-14：用户索要了一份面向 `openclaw-control-ui` 语境的《按阶段落地的字段映射表》，并以 Dreamina staged run 的真实日志为样本，要求把字段契约从聊天草案收成结构化的阶段映射产物。当前已沉淀的核心输出格式是“按 P0~P6 阶段分组的字段映射表 + 一张跨阶段统一字段契约表 + 一张建议落地顺序表”，重点覆盖 `proxyPrecheck / entry / credential / verification / profileCompletion / postAuthReady / accountDelivery` 七段。
- 2026-04-14：这轮字段映射产物里已经形成一个应长期保留的契约方向：`success/state/reason/nextStage/source(signal)/signalStrength/durationMs` 应作为跨阶段公共主字段；`state` 表示阶段原始事实态，`reason` 表示稳定可聚合原因码，`nextStage` 只在 finalize 后输出，`durationMs` 所有阶段必带。后续继续整理 staged architecture 文档时，默认以这组公共字段为主骨架，而不是让各阶段自由长字段。
- 2026-04-14：本轮还明确了 `detail` 处理原则的长期方向：不要再把所有临时信息都直接塞进顶层结果对象，后续应默认推进 `detail` 的稳定/调试分层，至少在文档中区分 stable detail 与 debug trace。尤其 S0 `proxyGrade/detail`、S1 `entryHealth/recoveryTrace`、S3 `retrySummary`、S5 `sessionObservationTrace`、S6 `accountSummary vs deliveryPayload` 都应按这个思路收口。
- 2026-04-14：用户贴出的最新批次 `dreamina-batch-2026-04-14T02-39-42-292Z` 再次验证了此前性能判断仍成立：首个账号总耗时约 `42053ms`，第二个账号约 `53585ms`；S1 `entry` 仍稳定卡在 `17835ms` 左右，S4 `profileCompletion` 仍约 `7.6s`，S6 `account-delivery` 维持在约 `57~62ms`，而 S3 `verification` 会因拉码长尾出现明显波动（本批一例约 `4551ms`，另一例约 `8588ms`）。长期上应继续把“固定慢的 S1”与“波动慢的 S3”分开治理，不要混成一个问题。
# 2026-04-14

- 2026-04-14：用户贴出批次 `dreamina-batch-2026-04-14T03-18-01-611Z` 的真实运行日志后，已再次确认当前 Dreamina staged 主链在 `proxyPrecheck -> entry -> credential -> verification -> profileCompletion -> postAuthReady -> accountDelivery` 七阶段下可稳定双号跑通，`successCount=2`，两号总耗时约 `42125ms` 与 `39355ms`。其中 S0 约 `7.0s/4.8s`，S1 仍约 `17471ms/16910ms`，S2 约 `3.8s`，S3 约 `4.3s~4.5s`，S4 约 `7.6s`，S5 约 `1.7s`，S6 约 `49ms/66ms`。这进一步坐实当前性能结构已收敛为“主瓶颈仍是 S1 entry 固定前置耗时”，而 S5/S6 已基本不是主要耗时点。
- 2026-04-14：针对 `dreamina-batch-2026-04-14T03-18-01-611Z` 的 batch artifact 再核对后，当前字段契约落地状态需要长期记住一个新结论：最终批次产物中 `deliveryPayload`、`stageResults.postAuthReady.detail.*`、`stageResults.accountDelivery.detail.*`、`sessionSummary`、`countryCode/countryName`、`sessionId` 已真实落盘，不再只是日志里可见；但 `successAccounts[*].detail` 目前仍为 `null`，且 summary 视图中的 `stageResults` 还主要集中在后段（尤其 S5/S6），前段 S0~S4 的 rich detail 没有完整透传到最终摘要层。后续若继续收口字段契约，默认优先补 summary/serialization write-through，不要误判为各 stage 本身没产出字段，更不要因此重构稳定主链。
- 2026-04-14：`session-records/latest.jsonl` 与 `session-records/latest.txt` 在该批次再次验证为“当前批次累计视图”语义，且本批只保留两条记录，内容已包含 `email / countryCode / countryName / sessionId / sessionSource`，说明 current-batch reset + in-batch accumulate + dedupe 的 latest 文件语义在真实运行中持续成立，后续不应再回退为全历史累计心智模型。
- 2026-04-14 11:42（Asia/Shanghai）：对新验证批次 `dreamina-batch-2026-04-14T03-35-34-356Z` 的最终 summary 做了运行后验收，确认 `stageSummary` 与 `slowestStage` 已成功写入 `Dreamina\batch-results\*.json` 和 `latest\dreamina-batch-latest.json`，说明 `87df4d1` 中的人类可读摘要层已生效。
- 同次验收确认一个仍未闭合的 summary 写透缺口：`successAccounts[*].resultFile` 与 `latestResultFile` 仍是空字符串，说明 `Dreamina-register.js` 到 `Dreamina-batch-runner.js` 的 `result.meta.filePath / latestByAccount` 元数据链路尚未真正落到最终 batch summary。后续若继续处理，应继续遵守用户边界，只补 summary/serialization/write-through，不碰稳定 stage 主链。
- 同批运行再次验证当前性能判断未变：两条成功链分别约 `47434ms` 与 `42916ms`，其中 `S1 entry` 仍是主慢阶段，代表值约 `16940ms` / `17228ms`；后续如果恢复性能优化，默认仍应把精细计时埋点优先放在 entry 打开页、健康检查、ready 确认几个子步骤，而不是继续只调 profile wait 参数。
# 2026-04-14

- 2026-04-14 11:55（Asia/Shanghai）：已完成对批次 `dreamina-batch-2026-04-14T03-48-36-127Z` 的 S1 entry 新增 `timingBreakdown` 首轮真实验收。该批次 `successCount=2`，两条成功链里 `entry` 仍分别为 `17402ms` 与 `17319ms`，继续稳居最慢阶段。
- 同批首个账号 `alicefarrington2020@tewsoneves.com` 的 `stageResults.entry.detail.timingBreakdown` 已明确给出：`openEntryPageMs=2066`、`checkEntryHealthMs=13005`、`confirmEntryReadyMs=2327`、`totalBeforeSettleMs=17400`。这条证据应作为后续 S1 优化的默认基线样本。
- 基于这轮新增细粒度打点，Dreamina S1 的长期判断需要更新为：当前 entry 的主耗时已不再是入口打开本身，也不是最后 ready 确认，而是 `checkEntryHealth` 这一步的固定长等待。后续若继续做低风险性能优化，默认优先审查和收紧 `shared-entry` 的 health-check 判定链路，不再继续把 `loginSignalStages` 档位微调当成主方向。
- 同轮日志也再次坐实一条方法论：对于像 S1 这种“总时长稳定但原因不清”的阶段，先加 observational timing breakdown 再决定是否改逻辑是有效路径。后续 Dreamina 性能治理应继续优先采用“先埋点定位，再定点小改”的策略，避免在未分清 `open / health / ready` 各段贡献前盲目压等待或重写主链。
# 2026-04-14

- 2026-04-14 12:13（Asia/Shanghai）：对批次 `dreamina-batch-2026-04-14T04-05-32-163Z` 的新一轮 S1 entry 运行结果做了验收，得到一个比前几轮更关键的长期结论：S1 不再表现为固定 `17s+` 单峰，而已经出现明显分化样本，同批内可同时出现 `entry=9922ms` 与 `entry=17492ms`。这说明当前 S1 的主问题不是单一固定等待常量，而是 Dreamina / CapCut auth surface 到达时机存在波动。
- 同批首个成功账号 `michellemclain1939@sungeingor.com` 的 `stageResults.entry.detail` 给出了新的关键证据：`timingBreakdown` 为 `openEntryPageMs=1597`、`checkEntryHealthMs=2136`、`confirmEntryReadyMs=6184`、`totalBeforeSettleMs=9919`，且 `signalTimeline=null`、`loginSignal=null`。这与上一批 `03:59:12` 中 `checkEntryHealthMs≈13.4s` 主导的样本形成鲜明对照，说明 S1 的慢点会在 `health` 和 `ready confirm` 两段之间漂移，而不是始终卡在同一个子步骤。
- 基于 `dreamina-batch-2026-04-14T03-59-12-819Z` 与 `dreamina-batch-2026-04-14T04-05-32-163Z` 的对比，Dreamina S1 当前应固定为新的长期判断：根因更像“auth gate / email gate / iframe 等真实入口信号到达具有抖动”，而不是 `checkEntryHealth(...)` 判定过于保守，也不是简单的 staged wait 配置过长。后续不要再盲目继续压 `loginSignalStages` 或盯着 overlay/login-signal patch 小修小补。
- 这轮还带来一条重要方法论更新：下一步 S1 工作仍应保持 observation-first，但观测重点要从“有没有 loginSignal”升级为“health 阶段和 ready 阶段各自到底首次命中了什么具体信号、命中时间和轮次是多少”。后续若继续优化，默认先补 `checkEntryHealth` 内部命中轨迹和 `confirmEntryReady` 内部 gate/email/input/iframe 首见轨迹，再决定该缩哪段等待或改哪条判定链，不要在证据不足时直接动稳定主链。
# 2026-04-14

- 2026-04-14 12:28（Asia/Shanghai）：已完成对批次 `dreamina-batch-2026-04-14T04-20-18-646Z` 的最新 artifact 验收，确认此前在 `Dreamina-register.js` 做的 observation write-through 修复已经生效。`batch-results/dreamina-batch-2026-04-14T04-20-18-646Z.json` 与 `latest/dreamina-batch-latest.json` 中，`stageResults.entry.detail.healthTrace` 与 `readyTrace` 不再是 `null`，说明 S1 观测链终于真正落到了最终产物层。
- 同批次给出一个新的关键长期结论：S1 的“快样本”已经有了可复核的结构化证据。首个账号 `rebecamoreno1985@shoulaqil.com` 的 `entry=9871ms`，其 `timingBreakdown` 为 `openEntryPageMs=1552`、`checkEntryHealthMs=2204`、`confirmEntryReadyMs=6110`、`totalBeforeSettleMs=9868`；第二个账号 `nataliereed1988@zweicpsu.com` 日志侧也显示 `entry=9588ms`。这说明当前 S1 确实存在稳定的约 `9.5s~9.9s` 快路径，不再只是偶发单点。
- 从 `rebecamoreno1985@shoulaqil.com` 的真实 artifact 看，当前这类快样本的具体信号链已经可以写死为：`healthTrace.decision=ready-signal-found`，`healthTrace.source=bodyText`，`healthTrace.value=dreamina`；随后 `readyTrace` 显示恢复链命中了两跳显式入口，先点击 `Sign in`，再点击 `Continue with email`，最终由 selector 命中 `EMAIL_GATE_READY`（`[class*="email"] input`）。后续分析 S1 时，不能再只盯着“Sign in 文本晚出现”这个旧单一假设，至少已有证据表明快路径里 health pass 是被页面 bodyText `dreamina` 提前放行，而真正后续耗时主要落在 gate 恢复链。
- 这轮同时也确认一个仍未闭合的观测缺口：虽然 `healthTrace` / `readyTrace` 已成功写透，但 `signalTimeline` 在该成功快样本里仍为 `null`。因此 S1 下一步默认不该再改行为逻辑，而应继续优先修 `signalTimeline` 的真实落盘与首见时序能力，特别是要把 auth CTA、continue-with-email、email input/gate 等信号的 first-seen elapsedMs 写实，避免继续只能靠 `readyTrace` 的结果态倒推过程态。

- 2026-04-14 12:49（Asia/Shanghai）：Dreamina `S1 entry` 的 `signalTimeline` 新一轮排查已进一步收敛。对成功批次 `D:\playwright\Dreamina\batch-results\dreamina-batch-2026-04-14T04-38-03-061Z.json` 与 latest 汇总的再次验证确认：`healthTrace`、`readyTrace`、`loginSignal` 已稳定写入最终 artifact，但 `stageResults.entry.detail.signalTimeline` 仍为 `null`。这说明问题不在 batch summary，也不只是 `Dreamina-register.js` success branch 透传，真实运行链本身仍未产出 timeline 数据。
- 2026-04-14 12:49（Asia/Shanghai）：本轮已确认当前实际命中的底层链路是 `shared-entry/dreamina/adapter.js` 的 `confirmDreaminaLoginGate() -> ensureDreaminaLoginGate()`，而不是先前带有更完整观察能力的另一套 staged wait 逻辑。`confirmDreaminaLoginGate()` 原先只返回 gate state，不返回 timeline；因此即使上层 wrapper 已尝试透传，最终 `signalTimeline` 仍会是空。
- 2026-04-14 12:49（Asia/Shanghai）：为避免继续误判为 summary/write-through 问题，本轮已把观察补丁下沉到真实运行链：在 `shared-entry/dreamina/adapter.js` 内为 `confirmDreaminaLoginGate()` 增加 `detail.loginSignal` / `detail.signalTimeline` 返回，并继续补到更底层的 `findVisibleReadyText(...)`、`findVisibleReadySelector(...)`，让 text/selector 命中本身可携带 `timelineSignals`。当前判断是，只要后续有有效账号再跑一轮 batch，就应能验证 `signalTimeline` 是否终于非空；若仍为空，再继续查这一条真实链，不再回头怀疑 summary 层。
- 2026-04-14 12:49（Asia/Shanghai）：后续验证被测试输入阻塞而非代码异常。两次批跑都因 `Dreamina/local-accounts.json` 无可用账号直接失败（`no accounts available`）。当前工作状态应视为：根因已从“透传丢失”进一步收敛到“真实运行链是否生成 timeline 原料”，代码已按 observation-only 原则补到真实链，剩余唯一关键验收动作是在补充新账号池后读取下一轮 batch artifact 中的 `stageResults.entry.detail.signalTimeline`。
- 2026-04-14：Dreamina S1 entry 观察链本轮已完成从“只记录 CTA first-seen”到“记录到 email input first-seen”的低风险补强。通过在 `shared-entry/dreamina/entry-adapter.js` 调整 `waitForDreaminaLoginEntryReady(...)` 的 observation 行为，最终 artifact `dreamina-batch-2026-04-14T05-33-32-473Z.json` 已稳定暴露 `entry.detail.signalTimeline.text:Sign in / continueWithEmailVisible / emailInputVisible`，并新增可用字段 `ctaSource=Continue with email`、`ctaOpenedGateMs=1200`、`postClickGateReadyMs=1200`。这说明 CTA 点击推进到 email gate ready 仅约 1.2s，不是 S1 约 19s 总耗时的主瓶颈。
- 2026-04-14：Dreamina S1 本轮阶段性诊断已更新，应把主要排查焦点从“continue layer -> email gate ready 是否过慢”转移到更前层的入口打开、健康检查、外层 gate-confirm/收口等待。当前最强结论是：`Sign in` 初见为 0ms，`Continue with email` 初见约 1200ms，`emailInputVisible` 初见约 2400ms，而整段 S1 仍约 19.4s，因此真正的大头更可能在 `openEntryPage`、`checkEntryHealth` 或 `confirmEntryReadyWithRecovery / gate-confirm` 外层等待，不应继续把 CTA 后半段视为主瓶颈。
# 2026-04-14

- 2026-04-14 14:27（Asia/Shanghai）：Dreamina `S3 verification-submit` 本轮在真实新批次 `dreamina-batch-2026-04-14T06-24-33-919Z.json` 中已拿到关键阶段证据，长期判断应更新为：当前 `S3` 的主要波动源不是输入框解析、验证码填入或提交确认，而是 `fetch-code` 本身。两条成功样本显示，首号 `fetchCodeMs=2409ms / resolveInputMs≈100ms / fillCodeMs=2079ms / confirmSubmitMs=7ms`，次号 `fetchCodeMs=14674ms / resolveInputMs≈102ms / fillCodeMs=2093ms / confirmSubmitMs=5ms`。这说明 `S3` 的显著长尾几乎全部来自 firstmail 拉码阶段；后续默认应优先把性能/稳定性分析焦点放在 provider polling 链，而不是误把锅甩给 fill/submit。
- 2026-04-14 14:30（Asia/Shanghai）：`S3` 旧排查方向已被这轮真实批次进一步修正。此前用户贴出的失败日志和早期 artifact 曾显示 `VERIFICATION_CODE_FETCH_FAILED` 与 `FIRSTMAIL_API_CODE_TIMEOUT`，但在修掉 `verificationPhaseTrace` 的 TDZ 崩溃后，新批次已双号完整成功，说明当前 `verification-submit` 主链、Dreamina 专用输入白名单和 direct-fill 主路径都处于可用状态。后续默认不要再优先怀疑 verification 主链骨架或验证码输入策略；真正需要继续优化的是 firstmail provider 的轮询成本与长尾等待。
- 2026-04-14 14:36（Asia/Shanghai）：`D:\playwright\firstmail-api.js` 本轮已确认存在一个应长期记住的低风险性能问题：`waitForDreaminaCodeViaApi(...)` 每轮轮询会串行执行 `fetchMessagesList(...) -> fetchUnreadMessages(...) -> fetchLatestMessage(...)` 三次 HTTP 请求，然后才进入候选合并和下一轮 sleep。这意味着在代理环境下，单轮拉码会被不必要地放大为“三次接口时延求和 + sleep”，直接推高 `fetchCodeMs` 长尾。后续只要再看见 `S3 fetch-code` 偏慢，默认第一优先就检查 provider 轮询里是否仍残留这种可并行却串行的网络扇出，不要先急着加更大预算或改验证码匹配语义。
- 2026-04-14 14:40（Asia/Shanghai）：针对上述 provider 长尾，本轮已落地一个低风险默认优化方向并应长期保留：firstmail 的 `messages / unread / latest` 三个接口查询应并发执行，再合并消息池与候选码，而不是维持串行探测。这个优化不改变业务语义、不改变候选码选择逻辑、不改变超时口径，属于纯降耗型 patch。后续若继续治理 `S3 fetch-code`，默认先沿“削掉每轮固定网络串行成本”继续小修，再决定是否进入更高风险的 polling 策略调整（如 source 分级、前快后慢、resend-aware）。
# 2026-04-14

- 2026-04-14 14:57~15:00（Asia/Shanghai）：Dreamina S1 entry 本轮“精修”已完成第二个低风险收口，并拿到真实双号成功批次 `dreamina-batch-2026-04-14T06-55-28-506Z.json` 作为验收证据。当前应把一条新的长期判断写死：S1 外层 `confirmEntryReadyMs` 的剩余固定尾巴，确实有一部分来自 `shared-entry/dreamina/entry-adapter.js` 中 CTA 点击后的保守 post-click observation wait；将该等待从更保守值收紧后，双样本 `entry.durationMs` 已进一步稳定到 `10211ms / 10170ms`，相较前一轮约 `11124ms / 11034ms` 再降近 1s，说明这刀是真收益，不是偶发抖动。
- 2026-04-14：同一批次还形成了一个对 S1 很重要的长期认知更新。即使在最新优化后，S1 也不是单一固定路径，而至少存在两类健康样本：一类能在较早 checkpoint 命中更快信号；另一类仍会在较后 checkpoint 才由 `AI Image` 之类 stronger text 放行。因此后续对 S1 的默认策略不应再是“继续盲压所有 wait 常量”，而应接受它已进入“约 10s 的阶段性健康停点”，除非后面有 artifact 证明某个外层等待还在稳定吞时，否则不再优先硬抠 S1。
- 2026-04-14：本轮还拿到一个重要的反证，应该长期保留。此前单次出现的 `CREDENTIAL_SUBMIT_NO_STATE_CHANGE` / `DREAMINA_CREDENTIAL_NO_STATE_CHANGE_AFTER_ALL_STRATEGIES`，在随后同批次与后续双号成功批次中均未复现，且用户贴出的完整运行日志显示两号都已成功跑穿 S2~S6。长期判断应固定为：当前这类 S2 no-state-change 更像账号/页面/代理瞬时态波动，不足以认定是 S1 精修造成的系统性回归；后续默认先观察是否连续复现，再决定是否单开 S2 分支处理。
- 2026-04-14 15:00（Asia/Shanghai）：最新双号成功批次的阶段耗时结构也应写入长期记忆，作为下一轮优化的默认基线。用户贴出的 `dreamina-batch-2026-04-14T06-55-28-506Z` 日志显示：S0 约 `4.9s~6.1s`，S1 已收敛到约 `10.1s`，S2 约 `3.9s`，S3 约 `7.5s~10.2s`，S4 约 `7.6s~7.7s`，S5 约 `1.7s~1.9s`，S6 极小。由此可见，在 S1 和 S3 都完成一轮有效低风险压缩后，当前新的“固定慢阶段”其实已经转移到 S4 profile-completion，而不是继续由 S1 独占主瓶颈。
- 2026-04-14 15:02（Asia/Shanghai）：Dreamina S4 profile-completion 本轮静态审计已形成新的长期诊断结论。`shared-profile-completion/dreamina/profile-completion-adapter.js::fillDreaminaBirthdayContinuousFlow(...)` 内部仍保留一串明显的保守固定等待，默认值包括 `birthdayYearPreClickWaitMs=1000`、`birthdayYearPreFillWaitMs=500`、`birthdayMonthPreClickWaitMs=1000`、`birthdayMonthOptionWaitMs=700`、`birthdayDayPreClickWaitMs=1000`、`birthdayDayOptionWaitMs=700`、`birthdayContinuousSettleMs=900`、`profileCompletionSubmitSettleMs=220`，仅这些固定 wait 总和就约 `6020ms`。结合真实批次中 S4 稳定落在 `7651ms / 7702ms`，当前长期判断应固定为：S4 的主要耗时不是业务判断本身，而是 continuous-flow 里这串保守尾等待。
- 2026-04-14：但对 S4 的默认优化方法也应一并写死，避免后续误操作。虽然固定等待总量很大，但 S4 当前成功率稳定，month/day dropdown 这类控件天然容易因等待过短而变 flaky，因此后续不能直接一口气砍 8 个 wait 常量。默认正确路径应继续保持 observation-first：先给 `fillDreaminaBirthdayContinuousFlow(...)` 增加 phase trace，把 `waitYearVisible / yearPreClickWait / yearPreFillWait / fillYear / waitMonthVisible / monthPreClickWait / monthOptionWait / pickMonth / waitDayVisible / dayPreClickWait / dayOptionWait / pickDay / continuousSettle / preSubmitSettle / clickNext` 这些子段真实耗时落入 artifact，再基于证据做单点、小幅、低风险压缩，而不是凭肉眼看代码直接大幅删 wait。

- 2026-04-14 16:03~16:10（Asia/Shanghai）：Dreamina `shared-batch-orchestration` 接入后的批处理状态同步问题，本轮已通过真实 `--concurrency 2 --account-limit 4` 跑批验证修复有效。此前 `Dreamina-batch-runner.js::onWorkerUpdate` 因 `batchContext.orchestration` 未初始化而报 `TypeError: Cannot set properties of undefined (setting 'queueSummary')`，现已通过在 `createBatchRunContext(...)` 初始化 `orchestration` 结构、并在 `onWorkerUpdate(...)` 增加兜底确保结构存在而修复。真实运行日志显示 `BATCH_OVERVIEW` 已能随 orchestrator 真状态变化正确更新，首轮双 worker 起跑时显示 `pending=2/running=2`，前两号完成后切换为 `pending=0/running=2/success=2`，最终 `successCount=4`，说明 overview 主状态源已基本统一到 orchestrator truth。
- 2026-04-14 16:03~16:10（Asia/Shanghai）：这轮验证还形成一条新的长期边界判断。当前 `BATCH_OVERVIEW` 已基本接上 orchestrator 真状态，但 `WORKER_OVERVIEW` 顶部的 `success=0/failed=0` 仍更像 worker-tracker 自身视角，而不是 batch summary 视角。后续默认不应急着把 batch 成功数硬塞进 worker tracker，以免混淆语义；更健康的边界应保持为“`BATCH_OVERVIEW` 管批次完成统计，`WORKER_OVERVIEW` 管 worker 当前运行态”。
- 2026-04-14 16:03~16:10（Asia/Shanghai）：针对最新批次 `dreamina-batch-2026-04-14T08-01-06-823Z.json` 的 S1 退化诊断，本轮已拿到一个应长期保留的关键结论。2 并发下前两条慢样本的 `entry.durationMs` 回升到 `16610ms/16648ms`，主因并不是 `openEntryPage` 或内层 gate-confirm 变慢，而是 `checkEntryHealth -> waitForDreaminaReady(...)` 再次掉回了 12s fallback 档。两条慢样本均显示 `openEntryPageMs≈2.0s`、`confirmEntryReadyMs≈2.0s~2.2s`，但 `checkEntryHealthMs≈12.3s~12.5s`，且 `healthTrace.waitTrace.steps=[0,800,2000,4000,12000]` 前四档均未命中，直到 `12000` 档才由 `matchedKind=strong-text / matchedValue=Sign in` 放行。这说明当前 2 并发下 S1 回升的主矛盾，是 health 阶段信号捕获太保守或信号到达被放大，而不是 CTA->email gate 链路本身变慢。
- 2026-04-14 16:03~16:10（Asia/Shanghai）：同一批次还进一步坐实了一个重要反差，应写入长期记忆。尽管 `checkEntryHealthMs` 吃满了 12s fallback，但同批慢样本的 `signalTimeline` 仍保持健康：`text:Sign in=0ms`、`continueWithEmailVisible=600ms`、`emailInputVisible=1200ms`，`ctaOpenedGateMs=600`、`postClickGateReadyMs=600`，且 `readyTrace.gateTrace.resolvedAtMs` 仅 `99ms/111ms`。长期判断应固定为：当前真正的问题不是 gate-confirm 链慢，也不是 CTA 点击后 email gate 打开慢，而是 `waitForDreaminaReady(...)` 的 health ready 判定，相比后续真实业务可继续的 timeline 更保守，导致页面其实已够继续时 health 仍被迫等到最后 fallback 档。
- 2026-04-14 16:03~16:10（Asia/Shanghai）：基于上述证据，Dreamina S1 下一步默认策略应明确为“继续 observation-first，优先补 `waitForDreaminaReady(...)` 每轮候选信号观测，而不是立刻改行为逻辑”。后续若继续治理 2 并发下 S1 退化，默认先在 `shared-entry/dreamina/adapter.js::waitForDreaminaReady(...)` 增加每轮已见但未被 health 接受的候选信号记录（如 Sign in、Continue with email、email input、body preview、关键 selector count），先确认是页面真实没到还是 health 判定条件过保守，再决定是否把某些已验证稳定的弱信号提前纳入 health pass 条件。不要在证据未补齐前直接重写 S1 主链或继续盲压 wait 常量。

- 2026-04-14 20:45~20:58（Asia/Shanghai）：Dreamina S1 health 本轮又形成一条高价值长期结论。基于 4 并发成功批次 `dreamina-batch-2026-04-14T12-43-26-120Z`，在把 `Explore Create Assets` 加入 health 强 ready 文本后，`EntryConcurrencyStats.checkEntryHealthMs` 已从此前约 `12535ms` 显著降到 `2259ms`，`matchedSignalBuckets` 也统一收敛为 `strong-text:Explore Create Assets`。这说明当前 4 并发慢 health 的真实主矛盾，不是页面必须等到更晚的 `dreamina` body-pattern 或 `AI Image` 文本，而是首页主区更早已出现的稳定强文本此前没被 health 链纳入。后续如果再遇到 S1 health 长尾，默认先审查 artifact 里 2s/4s 档的 `observedBodyPreview` 与真实首页主区文案，再决定是否补充更早且稳定的 home-ready 文本，不要先扩大 body-pattern。
- 2026-04-14 20:45~20:58（Asia/Shanghai）：同一批次也把另一个关键边界钉死了。尽管 health 已大幅改善，`EntryConcurrencyStats.confirmEntryReadyMs` 仍高达 `11344ms`，slow sample 普遍表现为 `outerConfirmMs≈3900~4800`、`waitTimelineMs≈11141~11507`、`waitResolvedPath=initial-gate-success`，且 `continueWithEmailVisibleMs≈3300~4200`、`emailInputVisibleMs≈3900~4800`。长期判断应固定为：在这批配置下，当前 S1 总耗时的大头已从 health 重新转回 `waitForDreaminaLoginEntryReady(...)` 的 confirm/wrapper 观察链，而不是 gate-confirm 本身。后续若继续压 S1，总方向应优先围绕 CTA / email-input 观察链做定点治理，而不是再回头动 health。
- 2026-04-14 20:52~20:58（Asia/Shanghai）：本轮又确认了一个很容易踩坑的语义边界，必须长期记住。最初为了让 health 更早放行，把 `Explore Create Assets` 同时并入了 entry 观察链可见文本后，后续验证发现这会把首页主区 ready 文本误当成“可点击登录 CTA”，从而让 `waitForDreaminaLoginEntryReady(...)` 在每轮可能去点击并不是真正登录入口的首页文案，再叠加 post-click wait，导致 confirm 不降反升。后续 Dreamina S1 的默认规则必须固定为：`home ready text != entry CTA text`。前者只应用于 health / readiness 判定，后者才允许参与 click action。不要再把首页主区文案和登录按钮候选混在同一条 clickable entryTexts 链里。
- 2026-04-14 20:58（Asia/Shanghai）：基于上述语义修正，本轮已对 `shared-entry/dreamina/profiles/dreamina-entry-profile.json` 与 `shared-entry/dreamina/entry-adapter.js` 做了结构性收口：`loginSignals.entryTexts` 保留真正可点 CTA 候选，新增 `loginSignals.readyTexts` 承载 `Explore Create Assets`、`Start Creating With AI Agent` 等首页 ready 文本；`detectDreaminaLoginEntrySignals(...)` 命中 `readyTexts` 时返回 `label=ready-text` 且 `clickable=false`。长期上应把这套边界视为默认正确实现，不再回退到“只要可见文本都当 entry CTA” 的粗糙模型。
- 2026-04-14 20:58（Asia/Shanghai）：同一轮验证还给出一个需要保留的优先级判断。上一轮新跑批次里出现的失败主因是 `DREAMINA_CREDENTIAL_STALLED_ON_SIGNUP=2`，而不是 health/confirm 语义拆分引起的结构性 S1 崩坏。后续若继续跟踪 1457cf1 之后的新批次，应优先看 `EntryConcurrencyStats.confirmAvg/confirmMax` 是否回落，并把 `DREAMINA_CREDENTIAL_STALLED_ON_SIGNUP` 视为独立的 S2/S3 瞬时问题观察项，不要因为它的出现而误判刚完成的 S1 文本语义分层一定回归。
- 2026-04-14 Dreamina S1 新一轮 headed/slow-mo 验证确认：此前新增的 final grace confirm（commit `8b0ff1a`, `fix: add final grace confirm for dreamina entry tail`）未实质改善 `DREAMINA_LOGIN_ENTRY_NOT_FOUND`，14:14 批次 `dreamina-batch-2026-04-14T14-14-16-243Z.json` 反而出现 4/8 失败，且全部失败桶收敛为 `DREAMINA_LOGIN_ENTRY_NOT_FOUND=4`。
- 这轮失败样本的长期结论已更明确：问题主因已不再是“浏览器过早关闭/再多等 1~2 秒就会出来”的纯时序尾巴，而是 Dreamina 首页壳下的登录入口发现策略缺口。典型失败样本（如 `janniewarren1920@sungeingor.com`、`joelhiles1947@xliiilas.com`）显示页面持续命中 `strong-text:Explore Create Assets`，但从头到尾没有真实 gate 信号（无 `Sign in` / `Continue with email` / email input），`openDreaminaLoginEntry(...)` 也找不到 entry，最终落在 `waitPath=recover-signals-gate-failed`。
- 因此，S1 下一步的默认优化方向应从“继续拉长等待/再加 grace 时间”切换为“补 homepage shell 专用的 entry probe / opener 覆盖”。只应在 `LOGIN_ENTRY_NOT_FOUND + strong home ready + 非错页` 条件下，尝试首页壳的 header / topbar / user-menu / avatar 一类入口触发，再重新执行 `confirmDreaminaLoginGate(...)`；不要回退到 `home-ready-text -> ENTRY_READY` 的错误捷径。

- 2026-04-14 22:47：在 `df19f62` 后对 `dreamina-batch-2026-04-14T14-38-08-272Z` 的两个失败产物（`janniewarren1920@sungeingor.com`、`warrenshannon1956@ttattdo.com`）完成首轮 clickable 证据核对。两者共同特征已确认：页面停留在 `https://dreamina.capcut.com/ai-tool/home`，标题正常，`bodyPreview` 均含 `Explore Create Assets`，但失败快照里仅看到首页内容按钮（如 `Auto`、`See results`、`Last one`、`Next one`，个别含 `AI Agent`），未产出任何可用登录入口线索。
- 2026-04-14 22:47：这轮 evidence-first 排查得到新的高优先级结论：当前 `DREAMINA_LOGIN_ENTRY_NOT_FOUND` 不能先按“文案改名/等待不足”处理，而应优先怀疑 `Dreamina-register.js::captureEntryDebugSnapshot(page)` 的 inventory 采集链本身没有真正产出 `clickableInventory / headerClickables / keywordClickables` 有效数据（当前读到的是空缺/未有效落盘）。后续默认先检查并修复该调试采集链，再决定是否调整 S1 entry opener/gate 逻辑，避免在缺乏真实 DOM 证据时继续猜测首页入口行为。
